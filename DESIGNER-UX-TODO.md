# 🎨 DESIGNER-UX — TODO (شکافِ موتور ↔ رابط)

برآمده از **بازبینیِ کاملِ محیطِ طراحی** (۱۴۰۴/۰۵/۰۸ — ۳۰ جولای ۲۰۲۶).
روشِ بازبینی: دیزاینر واقعاً در jsdom اجرا شد و مثل یک کاربر باهاش کار شد؛ هر یافته
**از کد یا از اجرا تأیید شده است**، نه حدس.

> **تشخیصِ اصلی:** موتور بسیار جلوتر از رابط است. کلی قابلیتِ ساخته‌شده و تست‌شده در
> `core` هست که از دیزاینر **اصلاً قابلِ دسترسی نیست**. بیشترِ آیتم‌های فاز ۱ صرفاً
> «UI روی کدِ از قبل تست‌شده»‌اند — کم‌ریسک‌ترین و پُربازده‌ترین کارِ کلِ مخزن.

**Legend:** ⬜ شروع‌نشده · 🚧 در حال انجام · ✅ انجام‌شده
**ریتمِ کار (هر آیتم):** ویرایش → `npm run designer:smoke` (+ `npm run test:core` اگر موتور
دست خورد) → `npm run format` → کامیتِ انگلیسی روی `master` → تیکِ باکس + نوشتنِ «انجام‌شده».

> ⚠️ **شمارهٔ خط‌ها** نسبت به نسخهٔ امروزِ فایل‌هاست؛ بعد از هر ویرایش جابه‌جا می‌شوند.
> این فایل مکمل است، جایگزینِ [TODO.md](TODO.md) و [NEXT-TODO.md](NEXT-TODO.md) نیست:
> آن دو دربارهٔ **موتور**اند، این یکی دربارهٔ **محیطِ طراحی**.

---

## 🔴 فاز ۰ — باگ‌های خاموش (بلاکرِ اعتماد)

الگوی مشترکِ هر چهار مورد: **خروجی خراب می‌شود و کاربر هیچ نشانه‌ای نمی‌بیند.**
خروجیِ غلطِ بی‌سروصدا اعتماد را می‌کشد — پس اول این‌ها.

- [x] **۰.۱ — مرزِ باند روی بوم + هشدارِ سرریز** ✅ 🔴 _مهم‌ترین یافتهٔ کلِ بازبینی_
  - **باگ (تأییدشده با اجرا روی موتور):**
    ```
    باند ۱ (ارتفاع ۶۰pt) → المان در y=200  →  رندر در y مطلقِ 230
    باند ۲ (بعدی)        → المان در y=0    →  رندر در y مطلقِ  90
    diagnostics: []      ← هیچ هشداری!
    ```
    محتوای باند اول **۱۴۰pt پایین‌تر از** محتوای باند دوم چاپ می‌شود؛ روی هم می‌افتند و
    ترتیبشان به‌هم می‌ریزد.
  - **چرا کاربر نمی‌فهمد:** [`renderCanvas`](apps/playground/designer/designer.js) فقط
    `#margins` را برای **کلِ A4** می‌کشد. باندِ ۶۰پونتی روی بوم انگار مالکِ کلِ صفحه است و
    هیچ خطِ مرزی ندارد.
  - **سناریوی واقعی:** کاربر لوگو را می‌کشد وسطِ صفحه (در حالِ ویرایشِ «سربرگ گزارش»)،
    همه‌چیز درست به‌نظر می‌رسد → در PDF نهایی لوگو روی جدول می‌افتد.
  - **کار:**
    1. یک خطِ مرزیِ باند روی بوم (در `y = band.height`) با برچسبِ ارتفاع.
    2. ناحیهٔ بیرونِ باند را کم‌رنگ/هاشور کن.
    3. المانی که از باند بیرون زده → کادرِ قرمز روی overlay + هشدار در نوارِ وضعیت.
    4. (تصمیم بگیر) آیا موتور هم باید `diagnostic` بدهد؟ اگر آری، در
       `layout/paginate.ts` وقتی `el.bounds.y + height > band.height`.
  - **پذیرش:** المانِ بیرون‌زده روی بوم **دیده** می‌شود؛ smoke که سرریز را می‌سازد و
    وجودِ نشانگر را assert می‌کند.
  - **انجام‌شده — سه لایه، از موتور تا نوارِ وضعیت:**
    **۱) موتور دیگر سکوت نمی‌کند.** `layoutBandImpl` حالا `maxBottom` را با ارتفاعِ حل‌شدهٔ
    باند مقایسه می‌کند و یک `warning` می‌دهد که **هر دو** عدد را نام می‌برد. این همان
    نقطه‌ای است که `renderToFile`/`renderBatch` هم از آن رد می‌شوند، پس کاربرِ Node هم
    محافظت شد نه فقط دیزاینر. دو ظرافت: `warnOnce` (باند در هر ردیف از نو چیده می‌شود و
    سندِ دارای ToC **دو بار** صفحه‌بندی می‌شود — بدونِ dedupe یک اشتباهِ نویسنده صدها بار
    گزارش می‌شد)، و **معافیتِ `background`/`watermark`** که طبقِ قرارداد ارتفاعِ جریان
    رزرو نمی‌کنند و قرار است کلِ صفحه را بپوشانند (این false positive را در تستِ موجودِ
    watermark گرفتم، نه در حدس).
    **۲) بوم.** `#bandBox` نوارِ متعلق به باند را می‌کشد با لبهٔ پایین + برچسبِ `NNpt`
    (ارقامِ لاتین، طبقِ تصمیمِ design-review ۲.۳)، و `#bandRest` بقیهٔ کاغذ را هاشور
    می‌زند. هر دو `pointer-events: none` و `z-index: 12` — بالای SVG موتور، **زیرِ**
    overlayها (۲۰)، پس هیچ کلیکی را نمی‌دزدند.
    **۳) المان و اکشن.** المانِ بیرون‌زده کلاسِ `.el.is-overflow` (کادر + هالهٔ `--danger`)
    می‌گیرد؛ مبنای مقایسه **ارتفاعِ واقعیِ رندرشده** از laid element است نه `bounds`، پس
    متنی که به شش خط شکسته هم شمرده می‌شود. در نوارِ وضعیت `#overflowInfo` مقدارِ سرریز
    را می‌گوید و **دکمه است نه برچسب**: یک کلیک ارتفاعِ باند را با محتوا جور می‌کند،
    به‌صورتِ یک command واگردپذیر، با toastِ «واگرد» درون‌خطی.
  - **تست:** ۶ تستِ واحدِ جدید (`paginate-extra.spec.ts` — fixed سرریز، جا شدن، auto که
    رشد می‌کند، auto با `max` که کلیپ می‌کند، معافیتِ watermark/background، یک هشدار برای
    ۳ ردیف) + بخشِ جدید در `smoke.js` که کلِ فلو را می‌راند (باند ۶۰pt → المان در y=200 →
    نشانگر + کلاس‌ها + diagnosticِ موتور → کلیکِ رفع → undo). **دو mutation test** زدم تا
    مطمئن شوم تست‌ها واقعاً می‌سنجند: حذفِ کلاسِ overflow و حذفِ diagnostic هر دو smoke را
    قرمز کردند.
  - **گیت‌ها:** eslint · prettier · typecheck · `jest --coverage` **۶۶۲ سبز** (۶۵۶ → +۶؛
    coverage 93.76/80.72/95.59/95.19 بالای آستانه) · build:core · build:angular ·
    smoke:tarball · designer:smoke — همه سبز. `CHANGELOG.md` هم آپدیت شد چون رفتارِ
    منتشرشده عوض شده.
  - ⚠️ **نیازِ یک نگاهِ چشمی:** jsdom موتورِ چیدمان ندارد، پس smoke فقط اعداد و کلاس‌ها را
    می‌سنجد نه ظاهر را. در مرورگر (**لایت و دارک**) این‌ها را ببین: هاشورِ `#bandRest` باید
    ملایم باشد نه چشم‌آزار، برچسبِ `NNpt` زیرِ خطِ مرزی و روی هاشور خوانا بماند، و در حالتِ
    سرریز هم خط و هم برچسب قرمز شوند.
  - **محدودیتِ آگاهانه:** `contentBottom` از `doc.pages[0]` خوانده می‌شود. برای باندِ
    `fixed` (همان حالتی که واقعاً می‌شکند) کامل است چون باندِ fixed هرگز split نمی‌شود؛
    برای باندِ `auto`ی بلندتر از صفحه ممکن است کم‌برآورد کند — که اشکالی ندارد، چون باندِ
    auto اصلاً سرریز نمی‌کند مگر `max` داشته باشد.

- [x] **۰.۲ — خطای بوم دیگر بی‌صدا نباشد** ✅
  - `catch (err) { pageSvgEl.innerHTML = ''; }` در `renderCanvas` — اگر layout خطا بدهد،
    بوم فقط **سفید** می‌شود، بدونِ هیچ پیامی. کاربر فکر می‌کند برنامه خراب شده.
  - **کار:** پیامِ خطا در یک نوارِ روی بوم (مثلِ `renderPreview` که این کار را می‌کند) +
    دکمهٔ «واگرد».
  - **پذیرش:** با یک عبارتِ خراب، بوم پیام می‌دهد نه سکوت.
  - **انجام‌شده:** `#canvasError` (یک `role="alert"` شناور در `canvas-wrap`، پس مستقل از
    زوم سرِ جایش می‌مانَد) با سه بخش: عنوانِ فارسی، راهنمای «چه کنم»، و `<code>`ِ پیامِ خامِ
    موتور به‌صورتِ `textContent` (نه `innerHTML` — پیام رشتهٔ قالب/موتور را حمل می‌کند).
    دکمهٔ **واگرد** تنها راهِ واقعیِ برگشت است، پس وقتی `store.canUndo()` نیست پنهان
    می‌شود به‌جای اینکه دروغ بگوید.
  - **اصلاحِ فرضِ خودم:** در فایل نوشته بودم «با یک عبارتِ خراب» — **غلط بود.** بررسی کردم:
    عبارتِ خرابِ نحوی، بارکدِ نامعتبر، symbologyِ ناشناس، دیتاستِ گم‌شده، رندرِرِ نبودهٔ
    custom و حتی نوعِ المانِ ناشناس هیچ‌کدام throw نمی‌کنند — سیاستِ «diagnosticِ غیرمهلک»
    درست کار می‌کند. throwِ واقعی **ساختاری** است. چهار مورد پیدا شد:
    چارتِ بدونِ `series` · باندِ بدونِ `height` · `elements` غیرِ آرایه · `bands` غایب.
    اولی **از دیزاینر قابلِ دسترس** است (importِ JSON یا خروجیِ کوپایلوت)، پس همان مبنای
    تست شد.
  - **تست:** بخشِ جدید در `smoke.js` که چارتِ بدونِ series را dispatch می‌کند و می‌سنجد:
    نوار ظاهر شود، detail خالی نباشد، `role="alert"` باشد، SVG پاک شود، دکمهٔ واگرد باشد،
    و کلیکِ واگرد هم المان را ببرد هم نوار را ببندد هم بوم را برگرداند. **mutation test**:
    با بی‌اثر کردنِ `setCanvasError(err)` تست قرمز شد.
  - **پیدا شد ولی نزدم (خارج از دامنهٔ ۰.۲):** `page.size` با نامِ کاغذِ ناشناس یا ابعادِ
    **منفی**، هر دو بی‌صدا رد می‌شوند (`diagnostics: 0`). همان الگوی «فاز ۰ راستگویی» است
    ولی مالِ موتور است نه بوم — به‌عنوانِ آیتمِ جدا ثبت شد (**۰.۵** پایین‌تر).

- [x] **۰.۳ — diagnostics زنده حینِ طراحی** ✅
  - الان diagnosticها **فقط بعد از کلیکِ «دانلود PDF»** نوشته می‌شوند (`diagEl`).
    عبارتِ اشتباه، بارکدِ نامعتبر، دیتاستِ گم‌شده — همه تا لحظهٔ خروجی مخفی می‌مانند.
  - **کار:** `layoutDocument` در `renderCanvas` از قبل صدا زده می‌شود؛ `doc.diagnostics` را
    بگیر و در نوارِ وضعیت (شمارنده) + تبِ داده نشان بده. کلیک روی شمارنده → المانِ مقصر
    انتخاب شود.
  - **پذیرش:** بایندِ اشتباه بلافاصله شمارندهٔ هشدار را روشن می‌کند.
  - **⚠️ نقشهٔ خودم را رد کردم — و mutation test ثابت کرد که حق داشتم.** در «کار» نوشته
    بودم از همان `layoutDocument`ِ `renderCanvas` استفاده کن. آن فراخوانی فقط **باندِ فعال**
    را می‌چیند، پس شمارنده وقتی پاصفحه بایندِ خراب دارد و تو سربرگ را ویرایش می‌کنی
    می‌گفت «۰ مشکل» — یعنی **دقیقاً همان دروغِ خاموشی** که این فاز برای کشتنش هست.
    به‌جایش یک pass جدا روی **کلِ سند** زدم. برای اثباتش، نسخهٔ باندی را به‌عنوان mutation
    گذاشتم و smoke قرمز شد («a diagnostic in another band was invisible…»).
  - **انجام‌شده:**
    **هزینه.** pass دوم روی هر تغییر، پس با **۱۵۰ms debounce**. ارزان‌تر از چیزی است که
    از قبل اجرا می‌شود: پنلِ پیش‌نمایش هر ۱۲۰ms یک `renderToSvg` (layout **و** paint)
    می‌زند؛ این فقط layout است. بوم حین درگ روان می‌ماند.
    **شمارنده.** `#diagInfo` در نوارِ وضعیت: کهربایی برای هشدار، قرمز با کلاسِ `has-error`
    وقتی خطا هست؛ ارقام لاتین. کلیک → تبِ داده + اسکرول به فهرست (و بازکردنِ کشویی روی
    چیدمانِ باریک).
    **فهرست.** `#diag` که قبلاً یک blobِ `pre-wrap` بود، حالا فهرستِ واقعی است: آیکونِ
    شدت + پیام (به‌صورتِ `textContent`) + دکمهٔ **«برو»**.
    **یک‌مکانی شدن.** هر چهار نویسندهٔ قبلیِ `diagEl.textContent` (JSONِ نامعتبرِ داده،
    importِ ناموفق، خطای ساختِ PDF، خروجیِ موفقِ PDF) از همان مسیر می‌روند —
    `showLocalDiag()` یا جایگزینیِ `liveDiags`. diagnosticهای export **مجموعهٔ فراگیرِ**
    زنده‌اند (فونت/تصویر فقط در paint می‌شکنند) پس جایگزین می‌شوند نه اضافه.
  - **«برو» چطور کار می‌کند (و کجا عمداً کار نمی‌کند):** `ExpressionDiagnostic` فقط
    `{severity, message, source?}` است و **`elementId` ندارد**. threading کردنش
    **۳۱ نقطهٔ فراخوانیِ `evaluateExpr` در ۱۲ فایل** و تغییرِ یک تایپِ منتشرشده است — پس
    به‌عنوانِ **۰.۶** ثبت شد، نه side quest. تا آن‌موقع `diagElementId()` از دو چیزِ
    موجود استفاده می‌کند: `'<id>'`ی که پیام‌های layout/paint نقل می‌کنند، و متنِ خودِ
    عبارت. اگر **دو** المان یک عبارتِ یکسان داشته باشند، `ambiguous` می‌شود و دکمه
    **نمایش داده نمی‌شود** — حدس‌زدن و انتخابِ المانِ اشتباه بدتر از نبودنِ دکمه است.
    `gotoElement()` در صورتِ لزوم باند را هم عوض می‌کند.
  - **تست:** بخشِ جدید در `smoke.js` (سندِ تمیز = بی‌شمارنده → بایندِ `sum(` → شمارنده +
    ردیفِ `sev-error` + کلاسِ `has-error` → «برو» انتخاب می‌کند، با assertِ اینکه از قبل
    انتخاب نبود → کلیکِ شمارنده تبِ داده را می‌آورد → **باندِ دوم**: هشدار حین ویرایشِ باندِ
    اول دیده می‌شود و «برو» باند را عوض می‌کند → JSONِ نامعتبر). چون debounce دارد، تست
    با `settle()` منتظر تایمر می‌ماند نه polling. **دو mutation test** سبز→قرمز.
  - **دستاوردِ جانبی:** `.diag` رنگش `#c77d1e`ِ literal بود (≈۳:۱ روی سفید) → `--warn`.
    ۱۰ جفت‌رنگِ جدیدِ این آیتم همه ≥۴.۵:۱ سنجیده شدند.
  - **گیت‌ها:** designer:smoke + prettier سبز؛ کامنت‌های CSS متوازن. موتور دست نخورد.
  - ⚠️ **نگاهِ چشمی:** شمارنده کنارِ چیپِ سرریز در نوارِ وضعیت (کهربایی و قرمز نباید قاطی
    شوند) و فهرستِ ردیف‌ها در تبِ داده، در لایت و دارک.

- [x] **۰.۶ — `elementId` روی diagnosticها** ✅ _(کشف‌شده در ۰.۳)_
  - `ExpressionDiagnostic` = `{severity, message, source?}`. هیچ diagnostic ای نمی‌گوید از
    **کدام المان** آمده، پس دیزاینر باید از متنِ پیام و عبارت حدس بزند و در حالتِ مبهم
    دکمهٔ «برو» را حذف کند.
  - **کجا:** فیلدِ **اختیاریِ** `elementId?: string` روی `ExpressionDiagnostic` +
    `EvaluationContext`، و پارامترِ اختیاریِ پنجم روی `evaluateExpr` — **۳۱ نقطه در ۱۲ فایل**
    (`leaf-layout`, `table-layout`, `chart-resolve`, `richtext-layout`, `crosstab-layout`,
    `grouping`, `report-variables`, `image-resolve`, `qr-resolve`, `dataset-resolver`,
    `paginate`).
  - **پذیرش:** هر diagnosticِ برآمده از یک المان `elementId` دارد؛ `diagElementId()`ِ دیزاینر
    به یک lookupِ ساده جمع می‌شود؛ فیلد اختیاری است پس مصرف‌کنندهٔ فعلی نمی‌شکند.
  - **انجام‌شده:** `elementId?` روی `ExpressionDiagnostic` و `EvaluationContext` + پارامترِ
    اختیاریِ پنجمِ `evaluateExpr`. **هر دو** نقطهٔ push آن را می‌گذارند: `evaluator.ts`
    (هشدارِ زمانِ اجرا) و `compile.ts` (خطای parse). `evaluateCompiled` از قبل `{...ctx}`
    را spread می‌کرد، پس فیلد بی‌دردسر عبور می‌کند.
    **۲۳ از ۳۱** نقطه المان دارند و گرفتند: متن، فیلد، بارکد، QR، تصویر، چارت (دسته‌ها +
    سریِ هر ستون)، جدول (header/detail/footerِ تجمیعی)، crosstab، رانِ richText، استایلِ
    شرطی، `visibleWhen`/`printWhen`، لینک، بوکمارک، colorScale/dataBar/iconSet، فیلدِ فرم،
    المانِ custom، زیرگزارش. `cellText`/`footerText`/`layoutParagraph`/`runText` پارامترِ
    `elementId` گرفتند چون خودشان المان را نمی‌دیدند.
    **۸ نقطه عمداً بدون id ماندند** چون واقعاً به هیچ المانی تعلق ندارند: ۵ موردِ
    `dataset-resolver` (اعلانِ دیتاست)، ۱ موردِ `grouping` (groupKeyِ باند)، ۲ موردِ
    `report-variables` (`template.variables`). همین است که فیلد را **اختیاری** می‌کند.
    ضمناً سه هشدارِ layout که از قبل نامِ المان را در متن می‌آوردند (crosstab، دو موردِ
    تصویر، زیرگزارش) حالا فیلد را هم دارند.
    **دیزاینر:** `diagElementId()` اول `d.elementId` را می‌خواند؛ دو fallbackِ قبلی ماندند
    چون برای diagnosticهای بیرونِ دامنهٔ المان (که ممکن است در متن id نقل کنند) هنوز کار
    می‌کنند.
  - **⚠️ تستِ اولِ خودم غلط بود، نه کد.** برای هشدارِ «unsafe property» از `n.__proto__` و
    `n.constructor` استفاده کردم؛ هر دو تست قرمز شد. علتش این بود که **lexer/parser** این
    نام‌ها را همان اول رد می‌کند (`Expected property name after "."`)، پس هرگز به گاردِ
    زمانِ اجرا نمی‌رسند — که از نظرِ امنیتی بهتر هم هست. با یک probe دیدم `elementId`
    درست ست شده و فقط پیام چیزِ دیگری است. تست‌ها به `nosuchfn(n)` رفتند تا مسیرِ
    **warnِ زمانِ اجرا** واقعاً پوشش داده شود، و چارت روی خطای parse ماند — یعنی هر دو
    نقطهٔ push تست می‌شوند.
  - **تست:** ۴ تستِ واحد (فیلد → خطای parse با id و `source` سالم · جدول → هشدارِ زمانِ اجرا
    چند فریم عمیق‌تر · چارت → سریِ عددی · دیتاستِ اعلام‌نشده → **بدونِ** id) + assert در
    `smoke.js` که `elementId` جانشینِ `source` نشده بلکه کنارش آمده.
  - **گیت‌ها (برای ۰.۵ و ۰.۶ با هم):** eslint · prettier · typecheck ·
    `jest --coverage` **۶۶۹ سبز** (۶۶۲ → +۷؛ coverage 93.8/81/95.49/95.2، `page-size.ts` و
    `evaluate.ts` هر دو ۱۰۰٪) · audit · build:core · build:angular · smoke:tarball ·
    designer:smoke — همه سبز. `CHANGELOG.md` آپدیت شد (هر دو تغییرِ رفتارِ منتشرشده‌اند).

- [x] **۰.۴ — هشدارِ سرریزِ متن** ✅
  - **تأییدشده:** متنِ بلند در جعبهٔ ۸۰×۱۴ به **۶ خط** می‌شکند و بیرونِ جعبه کشیده می‌شود؛
    `diagnostics: []`. overlayِ انتخاب ۱۴pt می‌ماند، پس **دستگیره‌ها دیگر با چیزی که کشیده
    شده جور نیستند**.
  - **کار:** ارتفاعِ واقعیِ رندرشده را از laid element بگیر؛ اگر > `bounds.height` بود،
    نشانگرِ سرریز + دکمهٔ «قدِ جعبه را با محتوا تطبیق بده» (auto-fit).
  - **پذیرش:** سرریز دیده می‌شود و با یک کلیک اصلاح می‌شود.
  - 💡 زیرساختش در ۰.۱ ساخته شد: `laidBottom` از قبل ارتفاعِ **واقعیِ رندرشده** را دارد.
    این آیتم فقط باید همان را با `bounds.height` خودِ المان مقایسه کند (نه با مرزِ باند).
  - **انجام‌شده:**
    **بوم.** overlay **کشیده نشد** — و این تصمیمِ اصلی بود. جعبهٔ overlay همان
    boundsِ **قابلِ ویرایش** است و drag/resize دقیقاً همان را برمی‌گرداند؛ اگر کِشش می‌دادم،
    دستگیره‌ها روی مستطیلی می‌نشستند که هیچ‌کس مالکش نیست. به‌جایش `.el.is-clipped`
    (لبهٔ پایینِ خط‌چین) + یک `.el-clip-ghost` **زیرِ** جعبه که نشان می‌دهد رنگ تا کجا
    واقعاً می‌رسد. یعنی کاربر هم جعبه‌اش را می‌بیند هم واقعیت را.
    **اینسپکتور.** ردیفِ `.warn-row` کنارِ فیلدِ ارتفاع (همان‌جایی که اصلاح می‌شود) با
    عددِ دقیق («روی کاغذ ۲۹pt جا می‌گیرد، نه ۱۴pt») و دکمهٔ **«جا کن»** = یک
    `setElementBounds` واگردپذیر که فقط ارتفاع را عوض می‌کند، نه عرض.
  - **تصمیم: موتور diagnostic نمی‌دهد.** برخلافِ ۰.۱، auto-grow رفتارِ **مستندِ** موتور است
    (`TODO.md` صریحاً «auto-grow» را برای richText قابلیت می‌داند)، نه دروغ. هشدار روی هر
    متنِ رشدکرده کلِ قالب‌های سالم را پرِ warning می‌کرد. حالتی که واقعاً سند را می‌شکند از
    قبل در ۰.۱ گرفته می‌شود (سرریزِ باند). این آیتم دربارهٔ **وفاداریِ WYSIWYG** است، که
    مسئلهٔ UI است نه موتور.
  - **دامنه:** فقط `staticText`/`dataField`/`richText`/`pageField`. جدول و چارت عمداً بیرون
    ماندند — «جا کن» برایشان بی‌معناست چون قدشان به تعدادِ ردیفِ دادهٔ واقعی بستگی دارد.
  - **تست:** بخشِ جدید در `smoke.js` (متنِ بلند در جعبهٔ ۸۰×۱۴ → کلاس + ghost با ارتفاعِ
    مثبت + **assert که overlay کشیده نشده** + ردیفِ هشدار + کلیکِ «جا کن» + عرضِ دست‌نخورده
    - undo). **mutation test** سبز→قرمز.
  - **گیت‌ها:** designer:smoke + prettier سبز. (موتور دست نخورد، پس build/tarball لازم نشد.)

  - 🔴 **در مسیر یک رگرسیونِ a11y از کارِ خودم گرفتم — و یک گپِ موجود.** توکنِ `--warn` را
    که اضافه کردم، **سنجیدم** (نه چشمی) و معلوم شد:
    - `--warn` روی tintِ ۱۰٪ خودش در لایت = **۴.۳۸:۱** → زیرِ AA برای متنِ ۱۱px. → tint به ۶٪.
    - `#overflowInfo` (۰.۱) و `#canvasError` (۰.۲) هم همین مشکل را داشتند (۳.۷۲ تا ۴.۳۸).
      → همه به سقفِ **۸٪** رفتند؛ `<code>`ِ داخلِ نوارِ خطا هم tint روی tint بود (جمعاً ۱۶٪
      → ۴.۳:۱) و به `--surface`ِ مات رفت.
    - **`--danger` لایت از قبل ۴.۳۸:۱ روی سفیدِ خالی بود** — گپِ موجودِ مخزن که ۱.۱ فقط
      `--faint` را دیده بود. → `#d64545` → **`#c62f2f`** (۵.۴۶:۱). ⚠️ تغییرِ جزئیِ رنگِ
      برند، هم‌جنسِ کاری که ۱.۲ با accent کرد؛ نپسندیدی برمی‌گردانیم.
    - دو hoverِ موجود (`#quickbar button.danger`، `#deleteEl`) هم زیرِ AA بودند → ۸٪ و ۶٪.
    - **بزرگ‌ترین یافته:** `#page` در **هر دو تم سفید** است، ولی نشانگرهای بوم از توکنِ تم
      می‌آمدند. `--warn`ِ تیره روی کاغذِ سفید **۲.۱۱:۱** است — عملاً نامرئی. سه توکنِ
      **مستقل از تم** اضافه شد (`--paper-accent/danger/warn`، بدونِ override در دارک، مثلِ
      مقیاسِ تایپ) و همهٔ نشانگرهای روی کاغذ به آن‌ها رفتند.
    - **نتیجهٔ نهایی: هر ۲۷ جفت‌رنگ pass** — متن با آستانهٔ ۴.۵:۱ و نشانگرِ غیرمتنی با ۳:۱
      (که جداگانه اعمال شد، نه یکسان).

- [x] **۰.۵ — اندازهٔ کاغذِ نامعتبر بی‌صدا رد می‌شود** ✅ _(کشف‌شده در ۰.۲)_
  - `page.size` با نامِ کاغذِ ناشناس (`'NotAPaperSize'`) یا ابعادِ **منفی**
    (`{width: -10, height: -10}`) هیچ diagnostic ای نمی‌دهد — `resolvePageSize` بی‌صدا به
    یک پیش‌فرض می‌افتد. دقیقاً همان الگویی که [NEXT-TODO فاز ۰](NEXT-TODO.md) بسته بود:
    «مدل چیزی را قبول می‌کند که بی‌صدا چیزِ دیگری می‌شود».
  - **کجا:** `packages/pdf-studio/core/src/layout/page-size.ts` (موتور، نه دیزاینر).
  - **کار:** نامِ ناشناس → `warning` + افتادن به A4؛ عرض/ارتفاعِ ≤۰ → `warning` + clamp.
  - **پذیرش:** تستِ واحد برای هر دو؛ هیچ قالبِ موجودی هشدارِ تازه نگیرد.
  - **انجام‌شده — یک قاعده به‌جای دو:** `pageSizeProblem(size)` تازه (کنارِ resolver، تا
    قاعدهٔ fallback و متنِ پیام نتوانند از هم دور بیفتند) نامِ ناشناس **و** مساحتِ
    غیرمثبت/غیرمتناهی را می‌گیرد، و `resolvePageSize` برای **هر دو** به A4 می‌افتد.
    ⚠️ **از «clamp» که در آیتم نوشته بودم منصرف شدم:** clamp کردنِ `-10` به یک صفحهٔ
    مویی فقط سرگیجه را به پایین‌دست منتقل می‌کند؛ A4 سند را قابلِ رندر و چاپ نگه می‌دارد.
    هشدار در `planSection` زده می‌شود — همان یک نقطه‌ای که هم صفحهٔ سند و هم **هر section**
    از آن رد می‌شوند — با همان `warnOnce`ِ ۰.۱. `pageSizeProblem` صادر شد تا ابزارها بتوانند
    قبل از رندر اعتبارسنجی کنند.
  - **تست:** ۳ تست (نامِ ناشناس → هشدار + ابعادِ A4 · مساحتِ منفی → هشدار + A4 · سکوت برای
    هر ۵ نامِ استاندارد و یک سایزِ سفارشیِ واقعی). آخری همان شرطِ «هیچ قالبِ موجودی هشدارِ
    تازه نگیرد» است. `page-size.ts` به **۱۰۰٪** پوشش رسید.

---

## 🩹 وصله‌های برآمده از بازبینیِ چشمیِ خودت

- [x] **و.۱ — چیدمانِ شکستهٔ اینسپکتور در پنلِ باریک** ✅ _(از اسکرین‌شاتِ علیرضا)_
  - **دو باگ در یک تصویر:**
    **الف) گریدِ `x/y/w/h` نامتوازن بود** (حدودِ ۳:۱ — `x` پهن، `y` باریک).
    علت: `grid-template-columns: 1fr 1fr`. در CSS Grid، `1fr` یعنی
    `minmax(**auto**, 1fr)` — یعنی هر track کفِ min-contentِ آیتمش را نگه می‌دارد.
    تا وقتی پنل جا دارد دو ستون مساوی‌اند، ولی همین‌که فضا کم شد تراک‌ها **دیگر
    مساوی نمی‌مانند** و یکی چند برابرِ همسایه‌اش می‌شود. → `repeat(2, minmax(0, 1fr))`
    که کف را صفر می‌کند و در **هر** عرض دقیقاً مساوی می‌مانند.
    **ب) ردیفِ «قفل» جمله‌اش در ۵ خط می‌شکست.** علت قطعی و از خواندنِ کد: ردیفِ چک‌باکس
    **دو** `<label>` دارد — عنوانِ فیلد و `.chk` که جعبه و جمله را می‌پیچد — و قاعدهٔ
    `.row label { flex: 0 0 56px }` **هر دو** را می‌گرفت، پس `.chk` در ستونِ ۵۶پیکسلیِ
    عنوان فشرده می‌شد. → `.row > label.chk { flex: 1 1 auto; min-width: 0 }`.
  - **علاوه بر رفعِ علت، فضا هم اضافه شد** (پلهٔ ۶۲۰px): گاترِ `.sec` از ۱۶ به ۱۰،
    و عنوان‌های بلندِ فیلد **بالای** فیلد می‌نشینند به‌جای دزدیدنِ ۵۶px از عرضِ آن —
    به‌جز گریدِ `x/y/w/h` که عنوانش یک نویسه است و در جای خودش می‌ماند.
  - **⚠️ صداقتِ تشخیص:** برای (ب) دلیل را از کد **اثبات** کردم. برای (الف) الگوی
    `1fr` در برابر `minmax(0,1fr)` توضیحِ استاندارد و منطبق بر همان ۳:۱ است، ولی
    **خودم در مرورگر اندازه نگرفتم** — jsdom موتورِ چیدمان ندارد. پس به‌جای ادعا، ابزارِ
    اندازه‌گیری ساختم (زیر).
  - **`inspector-check.html` جدید:** دیزاینر را در ۵ عرض (۱۲۸۰/۹۰۰/۷۶۸/۴۲۰/۳۷۵) بار
    می‌کند، پنل را باز می‌کند، یک المان را انتخاب می‌کند و **پهنای واقعی** را می‌خواند:
    دو سلولِ گرید (باید ≤۱px تفاوت)، ردیفِ قفل (باید >۵۶px)، و سرریزِ افقیِ پنل (باید ۰).
    جدولِ سبز/سرخ خودکار — همان الگوی `responsive-check.html`.
  - **گاردِ رگرسیون در smoke:** jsdom نمی‌تواند عرض بسنجد، ولی می‌تواند بسنجد که این دو
    قاعده برنگشته‌اند. هر دو گارد با **mutation test** تأیید شدند (برگرداندنِ `1fr` و
    برداشتنِ `flex` هر کدام smoke را قرمز کرد).

---

## 🟠 فاز ۱ — «چیزی که ساختی را قابل استفاده کن»

**بالاترین ROI کلِ مخزن.** همه‌شان UI روی کدِ از قبل تست‌شده‌اند — تقریباً بدونِ ریسک،
با اثرِ فوری. اگر فقط یک فاز قرار است بزنی، این است.

- [x] **۱.۱ — پنلِ تایپوگرافی** ✅ 🔥 _(دامنه بر اساس اندازه‌گیری اصلاح شد — پایین را بخوان)_
  - اینسپکتور فقط ۴ کنترل دارد: `fontSize`, `color`, `bold`, `align(start/center/end)`.
    مدلِ [`Typography`](packages/pdf-studio/core/src/model/style.ts) این‌ها را دارد و
    **هیچ‌کدام در UI نیست:** `fontStyle` (ایتالیک) · `decoration` (زیرخط/خط‌خورده) ·
    `lineHeight` · `letterSpacing` · `verticalAlign` · `fontFeatures`.
  - **دردناک‌ترینش:** `HorizontalAlign` مقدارِ `'justify'` را دارد و
    **[ROADMAP ۱.۴](ROADMAP.md) (justify با کشیدهٔ فارسی/تطویل) کامل و تست‌شده است** —
    ولی از دیزاینر **قابلِ روشن‌کردن نیست**. یک قابلیتِ هدلاین که کاربر به آن نمی‌رسد.
  - **پذیرش:** هر property مدل یک کنترل دارد؛ `justify` روی بوم تطویلِ فارسی نشان می‌دهد.

  - **🔬 اول اندازه گرفتم، بعد کنترل ساختم — و نصفِ فرضِ این آیتم غلط بود.**
    فرضِ فاز ۱ این است که «موتور از قبل پیاده کرده، فقط UI کم است». برای این آیتم
    **درست نبود.** هر property را با رندرِ واقعی سنجیدم (diff گرفتن از خروجیِ SVG و از
    content streamِ inflate‌شدهٔ PDF نسبت به یک baseline):

    | property                   | SVG | PDF | نتیجه                                       |
    | -------------------------- | --- | --- | ------------------------------------------- |
    | `fontStyle: italic`        | ✅  | ✅  | **اضافه شد**                                |
    | `lineHeight`               | ✅  | ✅  | **اضافه شد**                                |
    | `align: justify` (کشیده)   | ✅  | ✅  | **اضافه شد — هدلاین**                       |
    | `decoration: underline`    | ✅  | ❌  | **واگراییِ painter — رفع شد، بعد اضافه شد** |
    | `decoration: line-through` | ❌  | ❌  | پیاده نشده → **۱.۱۰**                       |
    | `letterSpacing`            | ❌  | ❌  | پیاده نشده → **۱.۱۰**                       |
    | `verticalAlign`            | ❌  | ❌  | پیاده نشده → **۱.۱۰**                       |
    | `fontFeatures`             | ❌  | ❌  | پیاده نشده → **۱.۱۰**                       |

    اگر بدونِ سنجش پیش می‌رفتم، **چهار کنترل می‌ساختم که هیچ کاری نمی‌کنند** — همان
    دروغِ خاموشی که کلِ فاز ۰ برای کشتنش بود، این‌بار ساختهٔ خودم.

  - **🐞 واگراییِ زیرخط (باگِ واقعی، رفع شد):** `resolveTextStyle` همیشه `underline` را
    حل می‌کرد و **svg-painter همیشه می‌کشیدش**، ولی در `pdf-painter.ts` کلمهٔ `underline`
    **اصلاً وجود نداشت**. یعنی المانِ زیرخط‌دار در پیش‌نمایش زیرخط داشت و در PDF نداشت.
    چرا زنده مانده بود؟ چون تنها تستش (`paint-extra.spec.ts`) فقط سمتِ SVG را assert
    می‌کرد. `drawUnderline()` اضافه شد و به **هر دو** مسیر وصل شد: متنِ ساده و رانِ
    richText (که همان باگ را داشت).
    تستِ جدید `underline-parity.spec.ts` هر دو painter را به هم قفل می‌کند: در PDF باید
    یک stroke بیشتر از حالتِ بی‌زیرخط باشد، در SVG باید `text-decoration` بیاید، و
    متنِ بی‌تزیین باید **بایت‌به‌بایت** با `decoration: 'none'` یکی بماند.

  - **دو چیز که پروبِ اولم اشتباه گزارش کرد** (و اصلاح شدند):
    `lineHeight` را «پیاده‌نشده» نشان داد چون متنِ تستم **یک خطی** بود و
    `i * lineHeight` همیشه صفر می‌شد؛ با متنِ شکسته درست ظاهر شد.
    `justify` را هم «پیاده‌نشده» نشان داد چون متنِ تستم **لاتین** بود و کشیده فقط در
    اتصالِ حروفِ فارسی معنا دارد؛ با متنِ فارسی: **۷ کشیده** درج شد و
    `"این"` → `"ایـن"`.

  - **انجام‌شده (UI):** سه سوییچِ سبک (ضخیم/کج/زیرخط) در **یک ردیف** به‌صورتِ تایلِ
    `.tog` (لیبل دورِ چک‌باکسِ پنهان، پس کلِ تایل hit target است و state یک فرم‌کنترلِ
    واقعی می‌ماند، با `:focus-within` برای حلقهٔ فوکوس چون خودِ جعبه پنهان است)؛
    «چینش» با گزینهٔ چهارمِ **دوطرفه (کشیده)** و یک `tinyhint` که با انتخابش ظاهر می‌شود
    و صادقانه می‌گوید کجا دیده می‌شود و کجا نه؛ و «فاصلهٔ خطوط» که **خالی گذاشتنش مقدار
    را پاک می‌کند** نه اینکه صفر بنویسد.
  - **بدهیِ CSS که ریشه‌ای بسته شد:** `.row label` سلکتورِ **descendant** بود، پس هر
    لیبلِ تودرتویی را به ستونِ ۵۶پیکسلیِ عنوان می‌برد — همان چیزی که ردیفِ «قفل» را
    شکسته بود (و.۱) و حالا داشت `.tog` را هم می‌شکست. به `.row > label` تغییر کرد تا
    این تله بارِ سوم تکرار نشود.
  - **تست:** بخشِ جدید در `smoke.js` روی متنِ فارسیِ چندخطی: هر سه toggle مدل را
    می‌نویسند، `lineHeight` مقدار می‌گیرد و با خالی‌شدن **حذف** می‌شود، گزینهٔ `justify`
    وجود دارد و بعد از اعمال **واقعاً کشیده تولید می‌کند** (نه فقط در مدل بنشیند)، و
    راهنما نمایش داده می‌شود. **دو mutation test**: حذفِ گزینهٔ justify و قطعِ سیمِ italic
    هر دو smoke را قرمز کردند.
  - **گیت‌ها:** eslint · prettier · typecheck · `jest --coverage` **۶۷۲ سبز** (۶۶۹ → +۳) ·
    build:core · smoke:tarball · designer:smoke — همه سبز.
  - ⚠️ **نگاهِ چشمی:** تایل‌های سبک در لایت و دارک (حالتِ روشن = پس‌زمینهٔ accent با
    `--accent-ink`)، و یک متنِ فارسیِ چندخطی با «دوطرفه» روی بوم و در PDF.

- [x] **۱.۱۰ — چهار propertyِ تایپوگرافی که موتور پیاده نکرده** ✅ _(دو تا زده شد؛ دو تا به ۱.۱۲)_
  - `decoration: 'line-through'`، `letterSpacing`، `verticalAlign`، `fontFeatures` —
    هر چهار در `Typography` هستند، zod قبولشان می‌کند، و **هیچ‌کدام در هیچ painterی
    کشیده نمی‌شوند** (با رندرِ واقعی تأیید شد، نه از روی کد).
  - دقیقاً الگوی [NEXT-TODO فاز ۰](NEXT-TODO.md): «مدل چیزی را قبول می‌کند که painter
    پیاده نکرده و به‌جای خطا بی‌صدا چیزِ دیگری می‌کشد».
  - **کجا:** `paint-style.ts` (حل)، `svg-painter.ts` + `pdf-painter.ts` (رسم)، و برای
    `letterSpacing`/`verticalAlign` **اندازه‌گیری هم** لازم است (`layout/measure.ts`)،
    چون عرضِ خط و جای عمودیِ متن را عوض می‌کنند — پس این دو از دو تای دیگر بزرگ‌ترند.
  - **پذیرش:** یا درست رندر شوند یا در validation رد شوند؛ هر دو painter هم‌خروجی؛
    بعدش کنترل‌هایشان به اینسپکتور اضافه شود.
  - **پیشنهاد:** `line-through` ارزان است (کنارِ `drawUnderline` که همین حالا اضافه شد)؛
    `fontFeatures` را فقط اگر `tnum` (ارقامِ هم‌عرض) هدف باشد بزن، که برای جدولِ مالی
    واقعاً مفید است.
  - **انجام‌شدهٔ نیمه — دو تا از چهار تا، و بقیه عمداً جدا شد:**
    **`line-through` ✅** — `ResolvedTextStyle.strike`؛ در PDF یک rule در ارتفاعِ x-height
    (همان helperِ `drawUnderline` که به `drawRule` تعمیم یافت)، در SVG `text-decoration`.
    حالا زیرخط و خط‌خورده می‌توانند **با هم** در SVG بیایند.
    **`verticalAlign` ✅** — `verticalOffset(style, boxHeight, lineCount)` مشترکِ هر دو
    painter. نکتهٔ ظریف: layout جعبه را تا اندازهٔ متن **بزرگ** می‌کند، پس slack فقط وقتی
    هست که نویسنده عمداً جعبه را بلندتر گرفته باشد — یعنی دقیقاً همان‌جایی که این تنظیم
    معنا دارد، و برای بقیهٔ اسناد خروجی **بیت‌به‌بیت** ثابت می‌ماند.
    **اینسپکتور:** سوییچِ «خ» کنارِ B/I/زیرخط (هر سه mutually exclusive چون `decoration`
    یک مقدار بیشتر ندارد) + سلکتِ «تراز عمودی».
  - **⚠️ تستِ خودم دوباره غلط بود، نه کد:** فرض کرده بودم جعبهٔ ۲۰pt دورِ خطِ ۱۶٫۸پونتی
    slack ندارد. دارد (۳٫۲pt) و درست هم هست که جابه‌جا شود. حالتِ بی‌slack جعبه‌ای است
    **کوتاه‌تر** از متن، که layout آن را دقیقاً به قدِ متن می‌رساند.
  - **باقی‌مانده — به [۱.۱۲](#) منتقل شد، نه اینجا:** `letterSpacing` و `fontFeatures` هر دو
    **اندازه‌گیری** را عوض می‌کنند (پس شکستِ خط، پس بایتِ خروجیِ هر قالبِ موجود) و
    `letterSpacing` در PDF عملگرِ `Tc` می‌خواهد که pdf-lib تمیز در `drawText` نمی‌دهد.
    نصفه‌زدنشان از نزدنشان بدتر بود.
  - **تست:** ۴ تستِ جدید (rule در PDF + نشانه در SVG · جابه‌جایی برای middle/bottom و
    بی‌اثری وقتی slack نیست · **هم‌اندازه بودنِ جابه‌جایی در هر دو painter** با خواندنِ
    ماتریسِ `Tm` · بیت‌به‌بیت ماندنِ حالتِ پیش‌فرض). **mutation test**: با `bottom → slack/2`
    دو تست قرمز شد.

- [x] **۱.۲ — پنلِ جعبه و کادر** ✅ 🔥
  - برای مستطیل/بیضی فقط «رنگ پُری» هست. المانِ متنی **اصلاً** پس‌زمینه/کادر ندارد.
  - در [`BoxStyle`/`BorderSet`](packages/pdf-studio/core/src/model/style.ts) هست و در UI نیست:
    `border` (رنگ/ضخامت/**per-side**) · `border.style` (**dashed/dotted** — همین تازگی در
    [NEXT-TODO ۰.۵](NEXT-TODO.md) پیاده شد!) · `border.radius` (گردیِ گوشه) · `padding` ·
    `opacity`.
  - **پذیرش:** کادرِ خط‌چینِ تک‌ضلعی با گوشهٔ گِرد از UI ساخته می‌شود.

  - **🔬 دوباره اول اندازه گرفتم — و پروبِ اولِ خودم false positive داشت.** baselineِ اولم
    «المان بدونِ box» بود، پس برای `padding` تفاوتی که دیدم از `fill` می‌آمد نه از padding.
    با baselineِ درست (همان جعبه **بدونِ** آن خصیصه):

    | property              | SVG | PDF | نتیجه                 |
    | --------------------- | --- | --- | --------------------- |
    | `fill`                | ✅  | ✅  | اضافه شد              |
    | `border.all`          | ✅  | ✅  | اضافه شد              |
    | per-side (فقط پایین…) | ✅  | ✅  | اضافه شد              |
    | `style: dashed`       | ✅  | ✅  | اضافه شد              |
    | `style: dotted`       | ✅  | ✅  | اضافه شد              |
    | `opacity`             | ✅  | ✅  | اضافه شد              |
    | `border.radius`       | ✅  | ❌  | **واگرایی — رفع شد**  |
    | `padding`             | ❌  | ❌  | پیاده نشده → **۱.۱۱** |

  - **🐞 واگراییِ گردیِ گوشه (رفع شد):** `paintBox` از `page.drawRectangle` استفاده می‌کرد
    که گوشهٔ گِرد ندارد، پس `style.radius` حل می‌شد و **دور ریخته می‌شد**. تستِ موجودش
    (`paint-extra.spec.ts` — «renders opacity and rounded-rect radius») باز هم **فقط SVG**
    را assert می‌کرد؛ دقیقاً همان الگوی زیرخط در ۱.۱.
    حالا وقتی `radius > 0` جعبه به‌صورتِ یک **مسیرِ صریح** با `drawSvgPath` کشیده می‌شود.
    دو تصمیم: گوشه‌ها **بزیهٔ مکعبی** (`C`) نه کمان (`A`) — چون پشتیبانیِ `A` بینِ
    parserها فرق می‌کند ولی `C` همه‌جا هست (ضریبِ ۰٫۵۵۲۳ تقریبِ استانداردِ دایره)؛ و
    `clampRadius` که شعاع را به نصفِ ضلعِ کوتاه‌تر محدود می‌کند وگرنه کمان‌ها روی هم
    می‌افتند. **`radius = 0` همان مسیرِ `drawRectangle` قبلی را می‌رود**، پس جعبه‌های
    موجود بایت‌به‌بایت ثابت‌اند (تست دارد).

  - **انجام‌شده (UI):** یک بخشِ واحدِ **«جعبه و کادر»** برای **همهٔ** انواعِ المان به‌جز
    `line` (که از `stroke` می‌کشد، نه `box`). این جایگزینِ کنترل‌های پراکندهٔ قبلی شد
    (`fill` فقط برای مستطیل/بیضی، `cfill`/`cborder` فقط برای گروه) — یعنی **متن، فیلد،
    تصویر، بارکد، QR، چارت و جدول تا امروز اصلاً کادر نداشتند**.
    کنترل‌ها: پُری (چک‌باکس + رنگ) · رنگ کادر · ضخامت · سبک (توپر/خط‌چین/نقطه‌چین) ·
    **اضلاع** (چهار تایل ↑→↓←) · گردیِ گوشه · شفافیت (٪).
  - **🐞 یک نقصِ طراحیِ خودم که تست گرفت:** اول «اضلاع» را پیش‌فرض **خاموش** گذاشته بودم.
    چون اینسپکتور بعد از هر تغییر re-render می‌شود و ضخامت جایی برای ماندن نداشت،
    تایپ‌کردنِ ضخامت بدونِ انتخابِ ضلع **بی‌صدا صفر می‌شد** — یعنی «ضخامت بده، کادر بگیر»
    کار نمی‌کرد. مدل اصلاح شد: **ضخامت کلیدِ روشن/خاموش است** و اضلاع فقط انتخابِ لبه‌اند
    که وقتی کادری نیست پیش‌فرض هر چهار روشن‌اند.
  - **حفظِ کلیدهای نامدیریت‌شده:** پنل کلِ `box` را از روی کنترل‌ها بازمی‌سازد (چون
    `BorderSet` یک شکلِ واحد است و «فقط ضخامت» را نمی‌شود بدونِ دانستنِ اضلاع عوض کرد)،
    ولی از `Object.assign({}, e.box)` شروع می‌کند تا `padding`ِ یک قالبِ واردشده — که هنوز
    هیچ painterی نمی‌خوانَد — دور ریخته نشود.
  - **تست:** بخشِ جدید در `smoke.js`: ضخامتِ تنها → `all` · برداشتنِ یک ضلع → شکلِ per-side
    با حفظِ سبک · برگرداندنش → دوباره `all` · radius · opacity به‌صورتِ کسرِ ۰..۱ ·
    روشن/خاموشِ پُری · **`padding` زنده می‌مانَد** · ضخامتِ صفر کادر را می‌برد ولی radius را
    نگه می‌دارد · و در آخر **رندرِ واقعیِ PDF** با و بدونِ radius فرق می‌کند.
    ۴ تستِ واحدِ جدید در `underline-parity.spec.ts` (که حالا دو واگرایی را پوشش می‌دهد):
    منحنی در PDF، `rx` در SVG، بایت‌به‌بایت بودنِ جعبهٔ گوشه‌تیز، و clampِ شعاعِ بزرگ.
    **دو mutation test**: حذفِ حفظِ کلیدها و برگرداندنِ `drawRectangle` هر دو smoke را
    قرمز کردند.
  - **گیت‌ها:** eslint · prettier · typecheck · `jest --coverage` **۶۷۶ سبز** (۶۷۲ → +۴) ·
    build:core · designer:smoke — همه سبز.
  - ⚠️ **نگاهِ چشمی:** تایل‌های اضلاع، و یک مستطیل با گوشهٔ گِرد + کادرِ خط‌چین در
    **PDF واقعی** (نه فقط پیش‌نمایش) — چون همین مسیر تازه ساخته شده.

- [x] **۱.۱۱ — `padding` جعبه در هیچ painterی خوانده نمی‌شود** ✅ _(کشف‌شده در ۱.۲)_
  - `BoxStyle.padding` در مدل هست و zod قبولش می‌کند، ولی `ResolvedBoxStyle` اصلاً فیلدش
    را ندارد و خروجیِ SVG با و بدونِ padding **یکسان** است (تأییدشده با رندر).
  - برخلافِ radius، این **کارِ layout است نه paint**: padding باید متن را تورفته کند و
    عرضِ در دسترس برای شکستنِ خط را کم کند (`layout/measure.ts` + `leaf-layout.ts`)، وگرنه
    فقط یک جعبهٔ کوچک‌ترِ بی‌ربط می‌شود.
  - **پذیرش:** متن با padding تورفته شود و زودتر بشکند؛ هر دو painter هم‌خروجی؛ بعد کنترلش
    به همین پنلِ «جعبه و کادر» اضافه شود.
  - **انجام‌شده — و برخلافِ ۱.۲ و ۱.۱۰ این یکی واقعاً layout بود:**
    ماژولِ تازهٔ `layout/box-padding.ts` با `resolvePadding` و `paddedRect`. **چرا در
    `layout/` و نه کنارِ بقیهٔ resolverهای paint:** قاعدهٔ مخزن این است که paint از layout
    import کند نه برعکس ([NEXT-TODO ۰.۳](NEXT-TODO.md))، و padding هر دو طرف را لازم دارد.
    **سه جا عوض شد، نه یکی:**
    ۱) `leaf-layout` متن را در عرضِ **تورفته** اندازه می‌گیرد (پس خط زودتر می‌شکند) و
    ارتفاعِ auto را با padding عمودی بزرگ می‌کند؛ تطویلِ justify هم به همان عرضِ درونی
    وصل شد، وگرنه کشیده‌ها بیرونِ padding می‌رفتند.
    ۲) و ۳) هر دو painter متن را از `paddedRect` می‌کشند — هم مبدأ، هم عرضِ ترازبندی، هم
    ارتفاعی که `verticalOffset` (از ۱.۱۰) رویش حساب می‌کند.
    **`paddedRect` هرگز وارونه نمی‌شود:** padding پهن‌تر از خودِ جعبه به یک باریکهٔ ۱pt
    می‌رسد، نه عرضِ منفی که هر اندازه‌گیریِ بعدی آن را واقعی فرض کند.
  - **اینسپکتور:** فیلدِ «بالشتک» در همان پنلِ جعبه — همان جایی که در ۱.۲ عمداً خالی
    گذاشته بودم تا کنترلی نسازم که موتور نادیده می‌گیرد. مقدار به واحدِ جاری (۱.۶) است و
    صفر کلید را پاک می‌کند به‌جای ذخیرهٔ صفرها.
  - **تست:** ۶ تستِ واحد (جابه‌جایی در SVG · همان جابه‌جایی در PDF از روی ماتریسِ `Tm` ·
    **باریک‌شدنِ ستونِ شکستِ خط** · رشدِ ارتفاعِ auto · عدمِ وارونگی · بیت‌به‌بیت ماندنِ
    المانِ بدونِ padding) + پوششِ کنترل در smoke. **mutation test:** نادیده‌گرفتنِ padding
    در اندازه‌گیری → تستِ شکستِ خط قرمز شد.
  - **پاکسازی:** `isZeroPadding` را نوشته بودم «برای مسیرِ سریع» و هیچ‌جا صدایش نزدم.
    به‌جای نوشتنِ تست برای کدِ مرده، حذفش کردم — پوششِ ماژول به **۱۰۰٪** رسید.
  - **گیت‌ها:** eslint · typecheck · `jest --coverage` **۷۰۱ سبز** · prettier ·
    designer:smoke — همه سبز. هیچ قالبِ گالری padding ندارد، پس خروجیِ موجود دست نخورد.

- [x] **۱.۳ — آپلودِ لوگو / تصویر** ✅ 🔥
  - تنها ورودی‌های فایلِ کلِ دیزاینر: `application/json` و `application/pdf`
    (`#fileInput`, `#pdfInput` در `designer.html`). `resources.images` همیشه `[]` می‌ماند.
  - **الان کاربر باید چه کند:** یک data-URI بیس۶۴ را **داخل کوتیشن، در یک فیلدِ عبارت**
    تایپ کند. این رایج‌ترین کاری است که یک کسب‌وکار ایرانی با فاکتور می‌خواهد بکند.
  - **کار:** drop/انتخابِ PNG/JPEG → `FileReader` → ثبت در `resources.images` →
    المانِ `image` با `resourceId`. دکمهٔ «انتخاب تصویر» در بخشِ محتوای المانِ تصویر.
  - **پذیرش:** drag کردنِ یک PNG روی بوم = المانِ تصویرِ آمادهٔ چاپ، بدونِ تایپِ یک کاراکتر.

  - **🔬 اندازه‌گیری (این‌بار خبرِ خوب):** هیچ دروغِ خاموشی در این ناحیه نبود — هر مسیرِ
    شکست‌خورده diagnostic می‌دهد. ولی مسیرها یکسان نیستند:

    | مسیر                       | SVG | PDF                               |
    | -------------------------- | --- | --------------------------------- |
    | `resourceId` → PNG base64  | ✅  | ✅                                |
    | `resourceId` → JPEG base64 | ✅  | ✅                                |
    | `resourceId` → SVG base64  | ✅  | ❌ «The input is not a PNG file!» |
    | `source` → data-URI        | ✅  | ✅                                |
    | `source` → آدرسِ http      | ✅  | ❌ «URL images are not embedded»  |

    پس **SVG را دمِ در رد می‌کنم** با دلیل، به‌جای اینکه قبول کنم و سرِ چاپ بی‌صدا بیفتد.

  - **انجام‌شده — دو کامندِ جدیدِ core:** `ensureImageResource(image)` (idempotent، هم‌سبکِ
    `ensureStyles`) و `pruneImageResources()` که بایت‌های بی‌ارجاع را جارو می‌کند —
    وگرنه یک قالب برای همیشه مگابایت‌ها base64 یتیم حمل می‌کرد. جست‌وجوی ارجاع **بازگشتی**
    است (داخلِ گروه و `itemTemplate` هم می‌گردد) وگرنه لوگویِ داخلِ یک گروه پاک می‌شد.
  - **دیزاینر:** انداختنِ PNG/JPEG روی بوم، یا دکمهٔ «انتخابِ تصویر…» در بخشِ محتوای المانِ
    تصویر. بایت‌ها در `resources.images` می‌نشینند و المان با `resourceId` به آن اشاره
    می‌کند، پس **قالب خودبسنده است و بدونِ اینترنت چاپ می‌شود**. تعویضِ تصویر و «حذفِ
    تصویر» هر کدام یک `composite` واگردپذیرند که آخرش `pruneImageResources` می‌زند.
    ابعادِ ذاتیِ تصویر خوانده می‌شود تا جعبه با نسبتِ خودش بیفتد.
  - **🐞 دو باگ که تست گرفت، نه من:**
    **الف)** importer روی `probe.onload` منتظر می‌ماند تا ابعاد را بخواند. jsdom دیکودرِ
    تصویر ندارد و **نه onload می‌آید نه onerror**، پس import تا ابد معلق می‌ماند. گاردِ
    ۲۵۰ms اضافه شد — که در مرورگرِ واقعی هم برای فایلِ خرابی که دیکودر را هنگ می‌کند لازم
    است. ابعاد یک nicety است، نه شرطِ ورود.
    **ب)** drop بدونِ مختصاتِ معتبر (`clientX` غایب) → `bounds` با **NaN** → رندرِ PDF
    چند مرحله بعد می‌ترکید با پیامی که هیچ اشاره‌ای به این‌جا نداشت. `imageBox` حالا
    مختصات را با `Number.isFinite` می‌سنجد و در نبودش به `nextSpot` می‌افتد.
  - **تست:** ۶ تستِ واحد برای دو کامند (برگشت‌پذیری، idempotent بودن، یک‌قدمِ undo همراهِ
    `addElement`، جاروی انتخابی، ارجاعِ داخلِ گروه، no-op وقتی چیزی برای جارو نیست) +
    بخشِ جدید در `smoke.js` (ردِ SVG · انداختنِ PNG → منبع + المان · mime و payload ·
    **بایت‌های واقعی در PDF به‌صورتِ image XObject** · bounds متناهی · یک undo هر دو را
    می‌برد). **دو mutation test**: اجازه‌دادن به SVG و برداشتنِ گاردِ NaN هر دو smoke را
    قرمز کردند.
  - **گیت‌ها:** eslint · prettier · typecheck · `jest --coverage` **۶۸۲ سبز** (۶۷۶ → +۶) ·
    designer:smoke — همه سبز.
  - ⚠️ **نگاهِ چشمی:** یک لوگوی واقعی بینداز و **PDF را باز کن** (نه فقط پیش‌نمایش)؛
    نسبتِ تصویر و دکمهٔ «تعویض/حذف» را هم ببین.
  - **باقی‌مانده (عمداً):** فشرده‌سازی/تغییرِ اندازهٔ خودکارِ تصویرِ بزرگ. فعلاً سقفِ
    ۴ مگابایت با پیامِ روشن گذاشته‌ام؛ downsampling آیتمِ جدا در `TODO.md` است
    («Image DPI/downsampling»).

- [x] **۱.۴ — مدیرِ فونت** ✅ 🔥
  - **صفر** کنترلِ `fontFamily` در اینسپکتور (پروب). همهٔ `DEFAULTS` هاردکد `'Vazirmatn'`.
    شرکت‌ها ایران‌سنس / ایران‌یکان / ساحل می‌خواهند. موتور `resources.fonts` را کامل دارد.
  - **کار:** آپلودِ TTF/OTF → `resources.fonts` → دراپ‌داونِ خانوادهٔ فونت در «ظاهر»
    (با پیش‌نمایشِ فارسیِ هر فونت) → embed در PDF مثلِ مسیرِ Vazirmatn.
  - **پذیرش:** فونتِ آپلودشده هم روی بوم و هم در PDF دیده می‌شود.

  - **🔬 بزرگ‌ترین یافتهٔ فاز ۱: `resources.fonts` را هیچ‌کس نمی‌خواند.** مدل صریحاً
    می‌گوید منابع «در قالب حمل می‌شوند تا سند خودبسنده و قابلِ‌حمل باشد» (§۴) — ولی این
    وعده **پوچ** بود. اندازه‌گیری با اندازهٔ خروجی (که برخلافِ grep دروغ نمی‌گوید):

    | حالت                         | اندازهٔ PDF   | هشدار                             |
    | ---------------------------- | ------------- | --------------------------------- |
    | بدونِ فونت                   | ۱۳۰۶ بایت     | ⚠ WinAnsi نمی‌تواند «س» را کد کند |
    | **فقط در `resources.fonts`** | **۱۳۰۶ بایت** | ⚠ همان — یعنی کاملاً نادیده       |
    | از `opts.pdf.fonts`          | ۳۱۳۶ بایت     | ✅                                |

    یعنی قالبی که فونت را داخل خودش حمل می‌کرد، در Node با `renderToFile` **متنِ فارسی‌اش
    را از دست می‌داد**. این فقط باگِ دیزاینر نبود؛ باگِ محصول بود.

  - **⚠️ ابزارِ سنجشِ اولِ خودم غلط بود:** اول دنبالِ `/FontFile2` در بایت‌های خام گشتم و
    برای **هر دو** حالت `false` گرفتم — که با واقعیت جور نبود، چون دیزاینر عملاً PDF فارسی
    می‌سازد. علت: subset داخلِ streamِ **فشرده** می‌نشیند. به مقایسهٔ اندازه سوییچ کردم که
    قابلِ‌انکار نیست.
  - **انجام‌شده (موتور):** `withTemplateFonts` در `renderToPdf` فونت‌های `data`دارِ قالب را
    به لیستِ painter تزریق می‌کند. فونت‌های caller **آخر** می‌آیند تا کلیدِ خانواده را
    ببرند (اپلیکیشنی که بایت‌های واقعی را دارد می‌تواند override کند). فونتِ فقط-`url`
    هشدار می‌گیرد به‌جای اینکه بی‌صدا متن را ببازد.
    ضمناً `FontProvider.create` حالا **قبل از embed** ورودی‌ها را با کلیدِ family+variant
    یکتا می‌کند — وگرنه قالب+caller همان فونت را **دوبار** جاسازی می‌کردند و فقط دومی
    استفاده می‌شد (سنجیده شد: ۳۷۴۱ → ۳۱۳۶ بایت).
  - **دو کامندِ جدید:** `ensureFontResource` (idempotent) و `removeFontResource`.
    عمداً **prune خودکار ندارد** برخلافِ تصویر: تایپوگرافی به فونت با **نامِ خانواده**
    ارجاع می‌دهد نه با id، پس فونتِ بی‌ارجاع معمولاً همانی است که نویسنده تازه آپلود کرده
    تا استفاده کند، نه زباله.
  - **انجام‌شده (دیزاینر):** بخشِ «فونت‌ها» در تبِ لایه‌ها (کنارِ سبک‌ها) با آپلودِ TTF/OTF و
    حذف؛ دراپ‌داونِ **فونت** در «ظاهر» که خانواده‌های در دسترس را می‌دهد؛ نامِ خانواده از
    نویسنده پرسیده می‌شود (نامِ واقعی داخلِ جدولِ `name` فونت است و parse کردنش یعنی حملِ
    یک font parser به صفحه، برای رشته‌ای که نویسنده می‌داند).
  - **🐞 واگرایی‌ای که خودم می‌ساختم و جلویش را گرفتم:** بوم SVGِ موتور است که فقط **نامِ**
    خانواده را می‌نویسد و مرورگر باید بشناسدش. بدونِ کاری اضافه، فونتِ آپلودشده **در PDF
    درست چاپ می‌شد و روی بوم fallback نشان می‌داد** — دقیقاً همان واگرایی WYSIWYG که این
    دیزاینر برای نبودنش هست. `syncFontFaces()` قالب را به `@font-face`های صفحه آینه می‌کند،
    با کلیدِ محتوایی تا رندرِ دوباره بایت‌ها را دوباره parse نکند.
  - **تست:** ۵ تستِ واحد برای کامندها + **۴ تستِ جدید `template-fonts.spec.ts`** (جاسازی
    از `resources.fonts` بدونِ هیچ option ای · یک‌بار embed شدن وقتی caller هم همان را
    می‌دهد · هشدارِ فونتِ URLـی · بردنِ caller در تعارض) + بخشِ جدید در `smoke.js`
    (آپلود → فهرست → `@font-face` تزریق‌شده → دراپ‌داون → نوشتن در المان →
    **رندرِ واقعیِ PDF بدونِ هشدارِ کدگذاری** → حذف و undo).
    **دو mutation test**: قطعِ `withTemplateFonts` و حذفِ `syncFontFaces` هر دو smoke را
    قرمز کردند.
  - **گیت‌ها:** eslint · prettier · typecheck · `jest --coverage` **۶۹۱ سبز** (۶۸۲ → +۹) ·
    build:core · smoke:tarball · designer:smoke — همه سبز.
  - ⚠️ **نگاهِ چشمی:** یک فونتِ واقعی (ایران‌سنس/وزیر) آپلود کن و ببین **هم روی بوم و هم در
    PDF** همان فونت است. فهرستِ فونت‌ها هر تایپ‌فیس را با خودش نشان می‌دهد.
  - **محدودیتِ آگاهانه:** اندازه‌گیریِ متن هنوز با تخمین‌گرِ پیش‌فرض است، نه با متریکِ فونتِ
    آپلودشده. یعنی شکستِ خط ممکن است یکی‌دو پیکسل با چاپ فرق کند. وصل‌کردنِ
    `FontkitTextMeasurer` به `resources.fonts` **خروجیِ همهٔ قالب‌های موجود را عوض می‌کند**،
    پس آیتمِ جدا می‌خواهد (**۱.۱۲**).

- [x] **۱.۱۲ — اندازه‌گیریِ متن با متریکِ فونتِ واقعی** ✅ _(کشف‌شده در ۱.۴)_
  - `layoutDocument` با `SimpleTextMeasurer` (تخمینِ عرضِ نویسه) می‌چیند، ولی PDF با فونتِ
    واقعی چاپ می‌شود. پس محلِ شکستِ خط و `auto`ـی ارتفاعِ باند کمی با کاغذ فرق دارد.
  - `FontkitTextMeasurer` از قبل هست و در تست‌ها استفاده می‌شود؛ کاری که مانده وصل‌کردنش به
    `resources.fonts` است تا `renderToPdf` خودکار از آن استفاده کند.
  - ⚠️ **این خروجیِ هر قالبِ موجودی را عوض می‌کند** (و snapshotهای byte-determinism را)، پس
    باید آگاهانه و با به‌روزرسانیِ snapshotها زده شود — نه به‌عنوانِ زیرمجموعهٔ آیتمی دیگر.
  - **همین‌جا هم می‌مانند (از ۱.۱۰ منتقل شدند، به همین دلیل):**
    - **`letterSpacing`** — عرضِ خط را عوض می‌کند پس شکستِ خط را، پس بایتِ هر قالبِ موجود را.
      ضمناً در PDF عملگرِ `Tc` می‌خواهد که pdf-lib در `drawText` تمیز در اختیار نمی‌گذارد؛
      باید مسیرِ رسمِ متن بازنویسی شود یا operator دستی push شود.
    - **`fontFeatures`** (مثل `tnum` برای ارقامِ هم‌عرضِ جدولِ مالی) — نیازمندِ اعمالِ
      featureهای OpenType در fontkit **هم در اندازه‌گیری و هم در رسم**، وگرنه دو طرف
      واگرا می‌شوند.
    - هر سه یک ریشه دارند: **اندازه‌گیری و رسم باید با هم عوض شوند**، که همان کارِ این آیتم است.
  - **انجام‌شده — ولی فقط measurer؛ `letterSpacing`/`fontFeatures` باز ماندند (زیر).**
    `layoutDocument` حالا measurer را از `resources.fonts` + `options.pdf.fonts` می‌سازد،
    پس `renderToPdf`، `renderToSvg` و درختِ layout هر سه یکی می‌بینند.
    **تفاوتِ واقعی (اندازه‌گیری‌شده):** متنِ فارسی در جعبهٔ ۱۴۰pt با تخمین **۲ خط / ۲۸.۸pt**
    می‌شد و با متریکِ واقعی **۳ خط / ۵۶.۲۵pt** — یعنی موتور جایی می‌گفت «جا شد» که کاغذ
    سرریز می‌کرد. ارتفاعِ خط هم دیگر از ascent/descentِ فونت می‌آید نه ضریبِ ثابتِ ۱.۲.
  - **دو تصمیمِ ظریف:**
    ۱) **خانوادهٔ ناشناس روی تخمین می‌ماند.** `FontkitTextMeasurer` به‌تنهایی به
    **اولین** فونتش می‌افتد — یعنی Helvetica را با متریکِ فارسی می‌سنجید و دقیق صدایش
    می‌زد؛ دقیقاً همان جانشینیِ خاموشی که کلِ این فاز دارد حذفش می‌کند. `TemplateTextMeasurer`
    هر اندازه‌گیری را به آن‌که می‌تواند صادقانه جواب دهد می‌فرستد.
    ۲) **memoize.** بومِ تعاملی در هر حرکتِ ماوس دوباره layout می‌کند و پارس کردنِ TTF ارزان
    نیست؛ کلیدِ کش خودِ **ارجاعِ بایت‌ها** است، پس فونتِ متفاوت هرگز measurerِ کهنه نمی‌گیرد.
  - **⚠️ یک رگرسیون که خودم می‌ساختم و گرفتم:** بومِ دیزاینر `layoutDocument` را **بدونِ**
    فونت صدا می‌زد و فقط `downloadPdf` فونت می‌داد. قبلاً بی‌ضرر بود (همه تخمین می‌زدند)،
    ولی بعد از این تغییر یعنی **پیش‌نمایش خط را جایی می‌شکند که کاغذ نمی‌شکند** — وارونهٔ
    همان باگی که داشتم رفع می‌کردم. یک `renderOpts()` مشترک ساخته شد و به هر پنج مسیر
    (بوم، پیش‌نمایش، بندانگشتیِ گالری، diagnostics، دانلود) داده شد.
  - **تست:** ۱۰ تستِ واحدِ جدید (`template-measurer.spec.ts`) + assert در `smoke.js` که
    **تعدادِ خطِ کشیده‌شده روی بومِ واقعی** با شمارشِ متریکِ واقعی برابر باشد.
    **mutation test:** بی‌اثر کردنِ `renderOpts()` → «۲ خط کشیده شد، ۳ انتظار می‌رفت».
  - **چرا هیچ تستی نشکست:** تست‌های byte-determinism دو بار رندر می‌کنند و مقایسه — تغییرِ
    measurer هر دو را یکسان عوض می‌کند. تست‌های goldenِ i18n هم متن را با pdfjs استخراج
    می‌کنند نه هندسهٔ خط را. پس بلاست‌ریدیوس واقعی صفر بود، ولی **این را باید سنجید نه فرض
    کرد** — به همین دلیل قبل از هر چیز کلِ suite را اجرا کردم.
  - **گیت‌ها:** eslint · typecheck · `jest --coverage` **۷۱۱ سبز** (۷۰۱ → +۱۰) · audit ·
    build:core · build:angular · smoke:tarball · designer:smoke — همه سبز.

- [x] **۱.۱۳ — `letterSpacing` و `fontFeatures`** ✅ _(letterSpacing زده شد؛ fontFeatures با سنجش رد شد)_
  - حالا که measurer واقعی وصل است، این دو ممکن شده‌اند — ولی هنوز **هر دو طرف** را
    می‌خواهند:
    - **`letterSpacing`**: عرضِ خط را عوض می‌کند (پس شکستِ خط)، و در PDF عملگرِ `Tc` لازم
      دارد که pdf-lib از `drawText` در اختیار نمی‌گذارد — یا باید operator دستی push شود یا
      مسیرِ رسمِ متن بازنویسی شود.
    - **`fontFeatures`** (مثل `tnum`): fontkit در `layout()` featureها را می‌پذیرد، پس سمتِ
      اندازه‌گیری شدنی است؛ سمتِ PDF باید همان featureها را به فونتِ embedشده بدهد وگرنه دو
      طرف واگرا می‌شوند.
  - **پذیرش:** هر دو در هر دو painter، با تستِ parity مثلِ ۱.۱ و ۱.۲.
  - **انجام‌شده — `letterSpacing` ✅ · `fontFeatures` ❌ عمداً نزدم.**
    **`letterSpacing`:** اندازه‌گیری (هر دو measurer) + SVG (`letter-spacing`) +
    PDF (`Tc` از طریقِ `setCharacterSpacing` که pdf-lib صادر می‌کند). هر سه فاصله را
    **بعد از هر نویسه از جمله آخری** حساب می‌کنند — همان کاری که خودِ `Tc` و CSS می‌کنند —
    وگرنه جای شکستِ خط با جای رسم فرق می‌کرد.
    **ظرافتِ اصلی:** `Tc` جزوِ **text state** است و از بلاک‌های `BT`/`ET`ِ خودِ pdf-lib عبور
    می‌کند، یعنی اگر صفر نشود به **هر المانِ بعدی** نشت می‌کند. بعد از هر المان صفر می‌شود،
    و یک تست مخصوصِ همین نشت هست (mutation: حذفِ ریست → قرمز).
  - **⚠️ `fontFeatures` را با سنجش رد کردم، نه با حدس:** دو چیز را probe کردم —
    ۱) `PDFFont` هیچ APIی برای featureها ندارد (`encodeText` فقط متن می‌گیرد)، پس سمتِ PDF
    از راهِ pdf-lib بسته است؛ ۲) حتی `fontkit.layout('12345', ['tnum'])` روی Vazirmatn
    **دقیقاً همان عرض** را داد. پس پیاده‌سازی‌اش یعنی اندازه‌گیری فیچر را ببیند و رسم نبیند —
    واگراییِ تضمینی، همان چیزی که کلِ این فاز صرفِ حذفش شد. اگر روزی لازم شد، راهش
    fork کردنِ مسیرِ encodeی pdf-lib است، نه یک فلگِ ساده.
  - **تست:** ۵ تستِ parity (`Tc` در PDF + attribute در SVG · **ریست‌شدنِ `Tc`** ·
    دست‌نخوردنِ المانِ بعدی · زودتر شکستنِ خط · بیت‌به‌بیت ماندنِ متنِ بدون فاصله) +
    کنترلِ اینسپکتور با assertِ «صفر کلید را پاک می‌کند».
  - **گیت‌ها:** eslint · typecheck · `jest --coverage` **۷۱۶ سبز** (۷۱۱ → +۵) ·
    build:core · smoke:tarball · designer:smoke · prettier — همه سبز.

  - 🔴 **و یک رگرسیونِ کارایی که خودم در ۱.۱۲ ساخته بودم و اینجا گرفتم.**
    اجرای کاملِ suite به **۱۱۴۳ ثانیه** رسید و `golden-i18n` قرمز شد — همان فایلی که
    تنها که اجرا می‌شد **۱۱.۷ ثانیه** طول می‌کشید. یعنی ۱۰۰ برابر کندتر زیرِ بارِ موازی.
    **علت:** کشِ measurer در ۱.۱۲ **تک‌خانه** بود. هر بار که ستِ فونت عوض می‌شد، کش رد
    می‌خورد و کلِ TTFِ ۱۶۳ کیلوبایتی **از نو پارس** می‌شد. قبل از ۱.۱۲ اصلاً پارسی در
    مسیرِ layout نبود، پس این کندی کاملاً دست‌پختِ خودم بود.
    **رفع:** به‌جای کش کردنِ _measurer_، خودِ **فونتِ پارس‌شده** کش می‌شود — یک
    `WeakMap` روی ارجاعِ بایت‌ها (پس بافرِ آزادشده فونتش را هم با خود می‌برد) به‌علاوهٔ یک
    `Map` برای base64 → بایت، تا قالبی که فونتش را base64 حمل می‌کند همان ارجاع را
    بگیرد و کشِ پایین‌دستی را خنثی نکند. حالا حتی ردخوردنِ کش هم ارزان است.
    **نتیجه: ۱۱۴۳ثانیه → ۲۰.۵ثانیه** (خودِ jest هنوز «estimated 1129 s» می‌نوشت).
    یعنی suite حالا از قبلِ شروعِ این کار هم سریع‌تر است (~۷۱ثانیه).
    **درس:** کشِ تک‌خانه‌ای در مسیری که ورودی‌اش عوض می‌شود، بدتر از نبودِ کش است.

- [x] **۱.۵ — ویرایشگرِ حاشیه (margins)** ✅
  - بخشِ «صفحه» در `designer.html` فقط اندازه/جهت/RTL-LTR دارد. `patchPageSetup` حاشیه را
    پشتیبانی می‌کند ولی هیچ کنترلی نیست — حاشیه روی ۳۰pt قفل است.
  - برای فاکتورِ رسمی و سربرگ، حاشیه یک تنظیمِ **پایه** است.
  - **پذیرش:** چهار ضلع جدا + قفلِ «هر چهار یکی»؛ overlayِ `#margins` زنده حرکت کند.
  - **انجام‌شده:** شبکهٔ ۲×۲ (بالا/راست/پایین/چپ) + چک‌باکسِ «هر چهار طرف یکی» که پیش‌فرض
    روشن است. مقادیر به واحدِ جاری (۱.۶) نشان داده و خوانده می‌شوند. فیلدی که در حالِ تایپ
    است در `rerender` بازنویسی نمی‌شود، وگرنه مکان‌نما می‌پرد.
  - **تست:** قفل روشن → هر چهار با هم؛ قفل خاموش → فقط همان ضلع و بقیه دست‌نخورده؛ و
    assertِ اینکه محتوا **واقعاً جابه‌جا می‌شود** (`x` المانِ چیده‌شده = حاشیهٔ چپ).
    **mutation test:** بی‌اثر کردنِ قفل → قرمز.

- [x] **۱.۶ — سوییچِ واحد (mm / cm / pt)** ✅
  - `page.unit` در [مدل](packages/pdf-studio/core/src/model/page.ts) هست، UI ندارد.
  - **الان راهنما به کاربر می‌گوید mm را دستی ضرب در ۲.۸۳ کند**
    ([designer-guide §9](docs/designer-guide.md)) — برای برچسبِ ۱۰×۶ سانتی این یعنی ماشین‌حساب.
  - **کار:** ذخیره همیشه pt بماند (قراردادِ مدل)؛ فقط **نمایش/ورودی** تبدیل شود —
    ابعادِ صفحه، x/y/w/h، حاشیه، گرید.
  - **پذیرش:** سوییچ به mm همهٔ اعدادِ UI را تبدیل می‌کند و خروجیِ PDF بیت‌به‌بیت ثابت می‌ماند.
  - **🔬 سنجیده شد:** `page.unit` هیچ اثری روی خروجی ندارد — و این **باگ نیست**، مدل صریحاً
    می‌گوید «واحدِ نمایشِ UI؛ ذخیره همیشه پونت است». پس برخلافِ بقیهٔ آیتم‌های فاز ۱،
    این‌جا کارِ موتور صفر بود و کار فقط تبدیلِ ورودی/خروجیِ فیلدهاست.
  - **انجام‌شده:** `toDisplay`/`fromDisplay` روی ابعادِ سفارشیِ کاغذ، حاشیه‌ها، `x/y/w/h` و
    ارتفاعِ باند. واحدِ فعال کنارِ عنوانِ «قرارگیری و اندازه» و کنارِ فیلدهای صفحه نوشته
    می‌شود.
  - **تصمیمِ آگاهانه — چه چیزی تبدیل _نمی‌شود_:** اندازهٔ فونت، ضخامتِ کادر و شعاعِ گوشه.
    این‌ها اندازه‌های **تایپوگرافیک**اند و هر ابزارِ طراحی (Figma/InDesign) آن‌ها را بدونِ
    توجه به واحدِ خط‌کش در پونت نگه می‌دارد؛ نشان‌دادنِ فونتِ «۴٫۲ میلی‌متری» شبیهِ باگ
    می‌شود نه قابلیت.
  - **تست:** واحد در مدل ذخیره می‌شود · **پونتِ ذخیره‌شده دست نمی‌خورد** · فیلد به mm تبدیل
    می‌شود · عددِ تایپ‌شده به mm درست به پونت برمی‌گردد · واحد در اینسپکتور دیده می‌شود.
    **mutation test:** وارونه‌کردنِ ضریبِ تبدیل → قرمز.

- [x] **۱.۷ — locale سند: ارقام / تقویم** ✅ _(زبان عمداً بیرون ماند — پایین)_
  - `page.locale` (language/digits/calendar) هیچ UI ندارد. سندِ لاتین می‌تواند **جهت** را
    عوض کند ولی نه ارقام و نه تقویم را — همه از قالبِ اولیه fa/persian/jalali می‌مانند.
  - **پذیرش:** یک فاکتورِ انگلیسی با ارقامِ لاتین و تاریخِ میلادی، بدونِ دست‌زدن به JSON.
  - **🔬 سنجیده شد — از سه فیلدِ locale فقط دو تا اثر دارند:**
    `digits`: `۱٬۲۳۴٫۵` → `1,234.5` ✅ · `calendar`: `۱۴۰۵-۰۵-۰۸` → `۲۰۲۶-۰۷-۳۰` ✅ ·
    **`language`: هیچ تفاوتی در خروجی ندارد.**
    دنبالش رفتم: تنها مصرفش `directionForLanguage()` است، و آن هم فقط وقتی
    `direction: 'auto'` باشد **و المان هیچ متنی نداشته باشد** (چون متن اول sniff می‌شود).
    یعنی عملاً غیرقابلِ‌مشاهده. پس کنترلی برایش نساختم — همان قاعدهٔ همیشگی: چیزی که
    اثر ندارد، کنترل نمی‌گیرد. (ضمناً `direction` گزینهٔ `auto` را در UI ندارد، فقط
    rtl/ltr، پس آن مسیر اصلاً باز نمی‌شود.)
  - **انجام‌شده:** دو سلکتِ «ارقام» و «تقویم» در بخشِ صفحه. هر کدام فقط فیلدِ خودش را عوض
    می‌کند و `locale` را کامل بازنویسی نمی‌کند.
  - **تست:** هر دو ذخیره می‌شوند · **هیچ‌کدام آن‌یکی را پاک نمی‌کند** (یک شیءِ مشترک‌اند) ·
    و ترکیبِ میلادی+لاتین **واقعاً در تاریخِ رندرشده** ظاهر می‌شود (`2026-…`)، نه فقط در مدل.

  - **⚠️ یک تستِ ۱.۴ در سوییتِ کامل قرمز شد** (در انزوا سبز بود): تایم‌اوتِ ۵ثانیه‌ایِ
    پیش‌فرضِ Jest، چون هر تست دو-سه بار Vazirmatnِ کامل را subset می‌کند و زیرِ بارِ بقیهٔ
    سوییت از ۵ ثانیه رد می‌شود. همان چیزی که skillِ gates هشدار داده و دو specِ
    byte-determinism از قبل `180000` دارند. همان قرارداد اعمال شد — نه پایین‌آوردنِ استاندارد،
    چون این تست‌ها **درستی** را می‌سنجند نه سرعت را.

- [x] **۱.۸ — ۸ دستگیرهٔ تغییرِ اندازه + دستگیرهٔ چرخش** ✅
  - `renderCanvas` تنها **یک** دستگیره می‌سازد (گوشهٔ پایین). نه دستگیرهٔ لبه، نه چرخش روی
    بوم (چرخش فقط یک `input[type=number]` است).
  - این اولین چیزی است که هر کاربری ناخودآگاه امتحان می‌کند.
  - **کار:** ۸ دستگیره (۴ گوشه + ۴ لبه) با `Shift` = حفظِ نسبت، `Alt` = از مرکز؛ دستگیرهٔ
    چرخش بالای انتخاب با snap به ۱۵ درجه.
  - **پذیرش:** تغییرِ اندازه از هر هشت جهت + چرخشِ ماوسی؛ همه یک قدمِ undo.
  - **⚠️ نقشهٔ خودم را اصلاح کردم:** در آیتم نوشته بودم «`Alt` = از مرکز». **غلط بود** —
    `Alt` در این دیزاینر از قبل معنیِ **«snap را خاموش کن»** دارد و در
    [راهنمای دیزاینر](docs/designer-guide.md) هم مستند شده. برداشتنش یک میان‌بُرِ
    منتشرشده را می‌شکست، پس فقط `Shift` = **حفظِ نسبت** پیاده شد و «از مرکز» کنار گذاشته
    شد. (برای چرخش، `Shift` = پله‌های ۱۵ درجه، هم‌راستا با `step="15"`ِ اینسپکتور.)
  - **انجام‌شده:** `RESIZE_EDGES` نگاشتِ «هر گیره کدام لبه‌ها را می‌کشد» است، پس دستگیرهٔ
    شمال‌غربی **مبدأ را جابه‌جا و جعبه را کوچک می‌کند** (نه اینکه جعبه را ببرد)، و گیرهٔ
    لبه فقط یک محور را می‌راند. لبهٔ مقابل همیشه ثابت می‌ماند؛ `MIN_SIZE` جلوی وارونه‌شدنِ
    جعبه از داخلِ لبهٔ مقابل را می‌گیرد. snapping روی همان لبه‌ای اعمال می‌شود که کشیده
    می‌شود، نه همیشه روی راست/پایین.
  - **🐞 واگرایی‌ای که با آسان‌شدنِ چرخش آشکار شد:** overlayها هیچ‌وقت چرخانده نمی‌شدند.
    تا وقتی چرخش فقط یک فیلدِ عددی بود کسی نمی‌دید؛ با گیرهٔ چرخش، جعبهٔ انتخاب جایی
    می‌ایستاد که هیچ‌چیز کشیده نشده. `transform: rotate()` روی overlay اضافه شد —
    موتور حولِ **مرکزِ** المان می‌چرخاند و `transform-origin` پیش‌فرضِ CSS هم مرکز است،
    پس بدونِ تنظیمِ اضافه هم‌راستا شدند.
  - **جزئیاتِ چرخش:** زاویه از مرکزِ المان تا نشانگر با `atan2` — چون `y` صفحه به پایین
    رشد می‌کند، `atan2` از قبل ساعتگرد است، که همان جهتی است که مدل مثبت می‌داند. مقدار
    به بازهٔ ۰..۳۶۰ نرمال می‌شود تا اینسپکتور هرگز ۷۲۰ درجه نشان ندهد، و کلِ کشیدن با یک
    `coalesceKey` **یک قدمِ undo** است.
  - **تست:** وجودِ هر هشت گیره + گیرهٔ چرخش · `se` فقط اندازه را عوض می‌کند و مبدأ را نه ·
    `nw` مبدأ را می‌کشد و جعبه را کوچک می‌کند · `e` فقط عرض · `n` فقط ارتفاع و مبدأِ y ·
    کشیدنِ افراطی جعبه را وارونه نمی‌کند · `Shift` نسبتِ ۲:۱ را نگه می‌دارد · چرخشِ ربع‌دور
    ≈۹۰ درجه می‌دهد، overlay هم می‌چرخد، و undo برش می‌گرداند.
    **دو mutation test**: بی‌اثر کردنِ کشیدنِ لبهٔ چپ و حذفِ چرخشِ overlay هر دو قرمز شدند.
  - **🐞 یک تلهٔ تست:** اولین اجرا `121.05×60.53` داد به‌جای `120×60`. علت باگ نبود —
    دلتای نشانگر بر `zoom` تقسیم می‌شود و بخشِ قبلیِ smoke زوم را روی ۹۵٪ گذاشته بود.
    تست حالا اول `1:1` می‌کند.
  - ⚠️ **نگاهِ چشمی:** هشت گیره روی المانِ کوچک نباید روی هم بیفتند، و گیرهٔ چرخش با
    ساقه‌اش بالای لبهٔ بالا بنشیند. رنگشان از `--paper-accent` است (مستقل از تم، چون روی
    کاغذِ سفید می‌نشینند).

- [x] **۱.۹ — همگام‌سازیِ پالتِ فرمان با جعبه‌ابزار** ✅ _(۱۰ دقیقه)_
  - جعبه‌ابزار **۱۲** ابزار دارد، `Ctrl+K` فقط **۹** «افزودن…».
    **جدول، فیلدِ برچسب‌دار و فیلدِ صفحه در پالت نیستند** — یعنی رایج‌ترین المانِ گزارش
    (جدول) از پالت پیدا نمی‌شود.
  - **کار:** `paletteCommands()` را از همان `[data-add]`های DOM بساز تا دیگر واگرا نشوند.
  - **پذیرش:** smoke که تعدادِ `[data-add]` را با تعدادِ فرمانِ «افزودن» برابر assert می‌کند.
  - **انجام‌شده:** `toolboxCommands()` فهرست را از خودِ `.toolrail [data-add]` می‌سازد و
    برچسب را از `aria-label`ِ همان دکمه می‌خوانَد (هر ۱۲ ابزار از قبل «افزودن …» فارسی
    داشتند، برای screen reader). یعنی دو فهرست دیگر **نمی‌توانند** واگرا شوند — نه اینکه
    فقط این‌بار هم‌راست شده باشند.
  - **تست:** smoke برای **هر** دکمهٔ ریل می‌گردد که برچسبش در پالت باشد (پس افزودنِ ابزارِ
    سیزدهم بدونِ رسیدن به پالت، تست را می‌شکند)، و بعد فرمانِ «افزودن جدول» را واقعاً اجرا
    می‌کند: یک overlay اضافه شود، اینسپکتور `table` نشان دهد، و undo برش گرداند.
    **mutation test:** بریدنِ فهرست به ۹ تا → `FAIL: toolbox entry missing from the palette:
افزودن جدول (table)`.

---

## 🟡 فاز ۲ — ارگونومیِ محیط

- [x] **۲.۱ — بومِ صفحهٔ کامل (باندهای روی‌هم‌چیده)** ✅ 🏗️ _بزرگ‌ترین کارِ این فایل_
  - **مشکل:** `activeBandTemplate()` کلِ استکِ باند را با **یک** باندِ مصنوعی جایگزین
    می‌کند و آن را بالای صفحهٔ خالی می‌کشد. کاربر:
    - نمی‌تواند ستونِ «جمع کل» در پابرگ را زیرِ ستونِ جدول تراز کند؛
    - نمی‌بیند چقدر جای خالی مانده؛
    - ریتمِ عمودیِ سند را نمی‌تواند بسنجد؛
    - پنلِ «پیش‌نمایش» فقط‌خواندنی است و **به‌طور پیش‌فرض خاموش**.
  - **چرا مهم است:** حتی JasperReports Studio باندها را روی هم چیده و **قابلِ ویرایش**
    نشان می‌دهد. ضمناً گالریِ تو پر از فاکتور/سربرگ/کارت‌ویزیت/برچسب است — یعنی
    **اسنادِ تک‌صفحه‌ایِ چیدمان‌محور**، که برایشان بومِ کاملِ صفحه مدلِ درست است.
  - **کار (پیشنهاد):** بومِ واحد که همهٔ باندها را پشتِ‌سرِ هم می‌چیند، هر کدام با نوارِ
    عنوان و مرزِ خودش (فاز ۰.۱)؛ کلیک روی هر باند آن را «فعال» کند؛ overlayها همچنان
    band-relative بمانند تا commandها دست نخورند.
  - **پذیرش:** یک فاکتورِ کامل (سربرگ + ردیف + پابرگ) در یک نما دیده و ویرایش می‌شود.
  - **ریسک:** بالا — این تغییرِ معماریِ بوم است. **بعد از فاز ۰ و ۱** بزن، نه قبلش.
  - **تصمیمِ اصلی — استکِ باند را در موتور گذاشتم، نه در دیزاینر:** راهِ ساده این بود که
    دیزاینر خودش استک را بازسازی کند (سرصفحه را کنار بگذار، باندها را بپیمای، قواعدِ
    تکرار و شکستن را تقلید کن). این دقیقاً همان کلاسِ باگی است که کلِ این فایل داشت
    حذفش می‌کرد: دو نسخه از یک قاعده که با اولین تغییر از هم می‌پاشند. پس `LayoutPage`
    حالا `bands: LaidBand[]` می‌دهد — همان‌جا که flow واقعاً باند را گذاشته، با
    `continued` برای تکه‌های شکسته بینِ صفحات. (کامیتِ جدا: `ca0cfdd`، ۵ تست.)
  - **انجام‌شده در دیزاینر:** بوم کلِ صفحه را می‌کشد؛ باندِ فعال جعبهٔ ویرایشِ خودش را
    دارد و بقیه به‌صورتِ نوارِ برچسب‌دار کشیده می‌شوند. overlayها از `stripTop/stripLeft`
    شروع می‌شوند، پس **دقیقاً روی جایی می‌نشینند که موتور المان را کشیده** — نه جایی که
    یک پیش‌نمایشِ تک‌باندی نشان می‌داد. کلیک روی هر نوار همان باند را فعال می‌کند.
  - **باندِ تکرارشونده:** منبع یک‌بار وجود دارد ولی چند بار کشیده می‌شود، پس فقط نوارِ
    **اول** overlay و برچسب می‌گیرد (با `×۳`)؛ بقیه دیده می‌شوند ولی ویرایش نمی‌شوند —
    چون ویرایشِ ردیفِ ۳ در واقع ویرایشِ ردیفِ ۱ است. کلیکشان همان باند را فعال می‌کند.
  - **یک assertionِ کهنه از ۰.۱ افتاد:** «بقیهٔ کاغذ هاشور خورده باشد» وقتی درست بود که
    بوم یک باند روی کاغذِ خالی بود. حالا هاشورزدنِ «بقیه» یعنی خاکستری‌کردنِ خودِ سند.
    تست به عکسش برگردانده شد، با دلیلش نوشته در کنارش.
  - **۱.۱۲ هم اصلاح شد:** شمارشِ `<tspan>`ها فرض می‌کرد بوم فقط یک المان دارد. حالا
    برابرِ **کلِ خطوطِ صفحه** سنجیده می‌شود، و یک نگهبان اضافه شد که اگر fixture دیگر
    دو measurer را از هم تشخیص ندهد، تست بی‌صدا سبز نماند.
  - **چهار mutation test، هر چهار قرمز:** برگرداندنِ `stripTop` به `m.top` (overlay سرِ
    حاشیه)، نکشیدنِ نوارِ باندِ فعال (سه نوار به‌جای دو)، برنداشتنِ کلاسِ `is-repeat`،
    و از کار انداختنِ کلیکِ نوار.
  - **یک ناهماهنگی که خودِ CSS لو داد:** نوارِ تکراری `cursor: default` داشت («اینجا
    کاری نمی‌شود») ولی کلیکش باند را فعال می‌کرد. رفتار درست بود و **افوردنس** دروغ
    می‌گفت، پس CSS اصلاح شد نه رفتار.
  - ⚠️ **هنوز در مرورگر دیده نشده:** jsdom موتورِ چیدمان ندارد، پس هندسه‌ای که تست
    می‌سنجد اعداد است نه ظاهر. نوارها، برچسب‌ها و هاشورها را باید در مرورگر دید.

- [x] **۲.۲ — خط‌کش + راهنمای کشیدنی** ✅
  - پروب: **صفر** خط‌کش. snap فقط به گریدِ ۵pt (`GRID = 5`، هاردکد) و لبهٔ المان‌ها.
  - **کار:** خط‌کشِ افقی/عمودی با واحدِ جاری (۱.۶)، کشیدن از خط‌کش = راهنمای دستیِ ماندگار
    (در قالب ذخیره شود)، snap به راهنماها.
  - **پذیرش:** راهنمای دستی بعد از reload سرِ جایش است.
  - **انجام‌شده:**
    **تراز از روی ساختار، نه محاسبه.** خط‌کش‌ها و کاغذ در یک **گرید ۲×۲** نشستند
    (گوشه / خط‌کشِ بالا / خط‌کشِ کنار / صفحه). یعنی هیچ offsetی نیست که با اسکرول یا زوم
    از هم بیفتد — چیزی که با موقعیت‌دهیِ دستی تقریباً حتماً می‌شکست و در jsdom هم قابلِ
    تشخیص نبود.
    **گامِ مضربِ ۱/۲/۵.** `rulerStep` گام را در **واحدِ نمایش** انتخاب می‌کند نه در پونت،
    پس برچسب‌ها همان عددهایی‌اند که آدم می‌نویسد (۱۰mm، نه ۲۸٫۳۵pt). نردبانِ ۱/۲/۵
    تنها پیشرفتی است که هر گام مضربِ بعدی است، پس زوم هرگز برچسب‌ها را به مضرب‌های
    عجیب نمی‌اندازد. حداقلِ فاصله بر حسبِ **پیکسل** است، پس زوم که کم شود خط‌کش
    رقیق می‌شود نه اینکه برچسب‌ها روی هم بیفتند.
    **صفر روی لبهٔ محتواست، نه لبهٔ کاغذ** — آن‌طور که طراح اندازه می‌گیرد. ناحیهٔ حاشیه
    عددِ منفی می‌گیرد و همچنان روی خط‌کش هست.
    **راهنماها:** از خط‌کش بکش تا ساخته شود، بکش تا جابه‌جا شود، به بیرونِ کاغذ ببر تا
    حذف شود. در `snapEdges` **اولِ فهرست** می‌آیند، پس راهنمایی که عمداً گذاشته‌ای بر
    هم‌ترازیِ اتفاقی با یک المان اولویت دارد.
  - **⚠️ تصمیم: راهنماها در قالب ذخیره نمی‌شوند.** آن‌ها ابزارِ ویرایش‌اند نه بخشی از سند؛
    گذاشتنشان در قالب یعنی تغییرِ schema، مهاجرت، و اینکه هر JSONِ صادرشده یا لینکِ
    اشتراکی داربستِ یک نفرِ دیگر را با خود ببرد. کنارِ draft در localStorage می‌نشینند،
    پس شرطِ پذیرش («بعد از reload سرِ جایش») برقرار است.
  - **تست:** ۱۱ تستِ واحد برای `rulerStep`/`rulerTicks` (نردبانِ ۱/۲/۵ در هر زوم و واحد،
    جا داشتنِ برچسب‌ها، رقیق‌شدن با زوم، صفر روی حاشیه، پوششِ ناحیهٔ منفی، ماندن داخلِ
    کاغذ، فاصلهٔ یکنواخت، نبودِ «۱۰.۰») + بخشِ smoke که کلِ چرخه را می‌راند (وجودِ tickها،
    موقعیتِ صفر، ساختِ راهنما با درگ، **ماندگاری در localStorage**، و حذف با بردن به
    بیرونِ کاغذ). **mutation:** بی‌اثر کردنِ حذفِ بیرون‌از-کاغذ → «a guide dropped off the
    sheet was kept: 2».
  - **روی موبایل (۶۲۰px) خط‌کش‌ها جمع می‌شوند** — ۲۰px عرض و ارتفاع می‌گیرند و بدونِ ماوس
    هم راهنما کشیدنی نیست.
  - **گیت‌ها:** `npx jest` **۷۹۳ سبز** · designer:smoke · prettier · کامنت‌های CSS متوازن.
  - ⚠️ **نگاهِ چشمی:** ضخامتِ ۲۰px و اندازهٔ ۹px برچسب‌ها، خوانایی در دارک، و اینکه
    خطِ راهنما روی کاغذ دیده شود ولی سندِ چاپی را شلوغ نکند (روی PDF نمی‌رود — فقط overlay).

- [x] **۲.۳ — فیدبکِ عددیِ فاصله + گریدِ قابل‌تنظیم** ✅
  - حین درگ فقط خطِ راهنما نشان داده می‌شود، نه **عدد**. گریدِ ۵pt قابلِ تغییر نیست.
  - **کار:** برچسبِ فاصله بینِ لبه‌ها حینِ درگ (سبکِ Figma) + کنترلِ اندازهٔ گرید و
    خاموش/روشنِ نمایشِ گرید.
  - **پذیرش:** فاصلهٔ دو المان بدونِ حساب‌کردن خوانده می‌شود.
  - **انجام‌شده:**
    **بازخوردِ زنده.** حین درگ یک برچسب بالای الِمان مختصات (`x · y`) و حین تغییرِ اندازه
    ابعاد (`w × h`) را **به واحدِ جاریِ سند** نشان می‌دهد. قبلاً خطِ راهنما می‌گفت
    «هم‌تراز شدی» ولی نمی‌گفت با چه و در چه فاصله‌ای، پس گذاشتنِ یک الِمان روی اندازهٔ
    مشخص یعنی: بکش، رها کن، اینسپکتور را بخوان، اصلاح کن.
    **گریدِ قابل‌تنظیم.** `snapValue` گام را به‌عنوان **پارامتر** می‌گیرد (با پیش‌فرض، پس
    تست‌های موجود دست نخوردند) و کنترلش در بخشِ صفحه است، به واحدِ جاری.
    **صفر یعنی بدونِ شبکه، نه تقسیم بر صفر** — و چسبیدن به لبهٔ الِمان‌ها باقی می‌ماند،
    چون آن ربطی به گرید ندارد.
  - **⚠️ گرید هم مثلِ راهنماها در قالب ذخیره نمی‌شود** — ترجیحِ ویرایش است، نه محتوای سند؛
    دو نفر که یک قالب را ویرایش می‌کنند نباید مجبور باشند سرِ گرید به توافق برسند.
  - **تست:** ۵ تستِ واحدِ جدید برای `snapValue` (گامِ دلخواه، صفر = بدونِ شبکه، مقدارِ
    بی‌معنی → پیش‌فرض، اولویتِ لبه بر هر گرید) + بخشِ smoke که سه گام را می‌راند
    (۵ → ۲۵، ۲۰ → ۲۰، ۰ → ۲۳ دست‌نخورده)، وجود و محتوای برچسب را می‌سنجد، و اینکه
    بعد از رها کردن ناپدید شود و گام ذخیره شده باشد.
    **دو mutation test:** نادیده‌گرفتنِ `gridStep` → «a 20pt grid did not snap to 20: 25»؛
    حذفِ `showReadout` → «the readout did not appear during a drag».
  - **`docs/designer-guide.md` §۷ هم به‌روز شد** (بازخوردِ زنده، خط‌کش و راهنما از ۲.۲، گام شبکه).
  - **گیت‌ها:** `npx jest` **۷۹۷ سبز** · designer:smoke · prettier · کامنت‌های CSS متوازن.
  - ⚠️ **نگاهِ چشمی:** برچسبِ درگ (آبیِ توپُر روی کاغذ) نباید زیرِ نوارِ ابزار یا بیرونِ
    کاغذ برود — clamp شده ولی فقط در مرورگر دیده می‌شود.

- [x] **۲.۴ — ناوبریِ چندصفحه‌ای روی بوم** ✅ _(تا جایی که معماریِ فعلیِ بوم اجازه می‌دهد)_
  - پروب: هیچ کنترلِ صفحه‌ای نیست. سندِ سه‌صفحه‌ای فقط در پنلِ پیش‌نمایش دیده می‌شود.
  - **پذیرش:** بشود روی بوم به صفحهٔ ۲ رفت (حتی اگر فقط‌خواندنی باشد).
  - **⚠️ تنشی که باید صریح باشد:** بوم **یک باند** را می‌چیند (`activeBandTemplate`)، پس
    «صفحهٔ ۲ روی بوم» با معماریِ امروز اصلاً وجود ندارد — آن با **۲.۱** می‌آید. پس این
    آیتم را به چیزی که واقعاً کم بود بستم، نه به یک تظاهر.
  - **آنچه واقعاً کم بود:** دیزاینر **هرگز نمی‌گفت سند چند صفحه می‌شود.** تنها راهِ فهمیدنش
    باز کردنِ پیش‌نمایش و اسکرول‌کردن بود.
  - **انجام‌شده:** شمارندهٔ `۲ / ۵` در نوارِ وضعیت با دکمه‌های قبل/بعد. کلیک، پیش‌نمایش را
    باز می‌کند و همان صفحه را در دید می‌آورد — چون پیش‌نمایش تنها سطحی است که امروز
    واقعاً صفحه‌بندی می‌کند. سندِ تک‌صفحه‌ای اصلاً شمارنده نمی‌گیرد (نویز است).
  - **رایگان به‌دست آمد:** `runDiagnostics` از قبل کلِ سند را می‌چیند (۰.۳)، پس
    `pageCount` بدونِ هیچ pass اضافه‌ای از همان‌جا خوانده می‌شود.
  - **تست:** بخشِ smoke که یک سندِ **واقعاً چندصفحه‌ای** می‌سازد (باندِ ردیف روی ۴۰ ردیف)،
    و می‌سنجد: پنهان بودن برای سندِ تک‌صفحه‌ای، عددِ درست در برابرِ `pageCount`ِ موتور،
    غیرفعال بودنِ «قبل» در صفحهٔ اول، باز شدنِ پیش‌نمایش با «بعد»، و **clamp** در انتها.
  - **دو mutation test — و یکی‌شان کدِ زائد را لو داد:** ثابت‌کردنِ `docPageCount` → قرمز.
    ولی حذفِ clampِ داخلِ `goToPage` **هیچ تستی را قرمز نکرد**، چون `renderPageNav` خودش
    clamp می‌کرد. دو نگهبان که هیچ‌کس نمی‌تواند از هم تشخیصشان دهد وزن است نه ایمنی، پس
    یکی ماند — و با mutation ثابت شد که همان یکی بارِ کار را می‌برد (`10 / 3`).
  - **گیت‌ها:** `npx jest` **۷۹۷ سبز** · designer:smoke · prettier.

- [x] **۲.۵ — صفحهٔ «پروژه‌ها / خانه»** ✅
  - **تنها آیتمِ بازماندهٔ [DESIGN-REVIEW-TODO](DESIGN-REVIEW-TODO.md) (۱۹/۲۰)** — عیناً
    منتقل شد تا در یک جا دنبال شود.
  - الان کاربر مستقیم واردِ ویرایشگر می‌شود؛ مدیریتِ چند سند نیست.
  - **کار:** صفحهٔ خانه: اسنادِ اخیر، جست‌وجو، «سندِ نو»، ورود از گالری — روی همان
    localStorage/history موجود.
  - **پذیرش:** بشود بینِ چند سند جابه‌جا شد بدونِ از دست رفتنِ کار.
  - **مسئلهٔ واقعی که پیدا شد:** «سندِ نو» پیش‌نویس را پشتِ یک `confirm()` **پاک می‌کرد**.
    یعنی تنها راهِ شروعِ چیزِ تازه، نابود کردنِ چیزی بود که داشتی. گالری هم همین‌طور:
    انتخابِ قالب روی سندِ باز می‌نشست.
  - **انجام‌شده:** کتابخانهٔ سند روی `pdfstudio.docs.v1` + `pdfstudio.currentDoc`.
    «سندهای من» با جست‌وجو، تغییرِ نام، تکثیر و حذف. «سندِ نو» و انتخابِ قالب حالا
    سندِ **تازه** می‌سازند، نه جایگزین.
  - **مهاجرت:** پیش‌نویسِ تک‌نفرهٔ قبلی (`pdfstudio.draft`) به‌عنوان اولین سند برداشته
    می‌شود، پس کسی که وسطِ کار بود چیزی از دست نمی‌دهد. کلیدِ قدیمی دست‌نخورده ماند.
  - **یک تصمیمِ ظریف — پنل موقعِ باز شدن ذخیره نمی‌کند:** اولش `openDocsPanel` یک
    flush می‌زد تا ردیفِ سندِ باز درست نشان داده شود. ولی این **دقیقاً همان ضمانتِ ۲.۵ را
    پنهان می‌کرد**: mutation ای که ذخیره را از `openDoc` برداشت، **سبز ماند**، چون پنل
    خودش ذخیره کرده بود. حالا `docsForDisplay()` نام و تعدادِ **زنده** را از store
    می‌خواند، پس باز کردنِ پنل فقط‌خواندنی است و ذخیره تنها جایی می‌مانَد که واقعاً
    «ترکِ سند» آن‌جا اتفاق می‌افتد.
  - **دو تستِ بی‌اثرِ دیگر که mutation لو داد:** (۱) تستِ «کار گم نمی‌شود» ۵۰۰ms صبر
    می‌کرد، ولی autosave خودش ۴۰۰ms است — پس هر جور که بود ذخیره می‌شد. حالا ویرایش
    **داخلِ پنجرهٔ autosave** رها می‌شود. (۲) تستِ یکتاییِ نام، چون هیچ دو سندی نامِ
    پایهٔ یکسان نداشتند، هیچ‌وقت به تصادم نمی‌رسید.
  - **۹ mutation، هر ۹ قرمز.**
  - **یک باگِ a11y در کدِ موجود پیدا شد:** نگهبانی نوشتم که هر modal باید در تلهٔ فوکوس
    و زنجیرهٔ Escape باشد (هر دو **لیستِ دستیِ id** هستند). `#cloneReview` در **هیچ‌کدام**
    نبود — یعنی با Escape بسته نمی‌شد. اضافه شد، و حالا تست جلوی تکرارش را می‌گیرد.
    ⚠️ روشِ اولِ تستم هم غلط بود: Escape را روی `document` می‌فرستادم، ولی پالتِ فرمان
    روی inputِ خودش گوش می‌دهد — پس از داخلِ modal فرستاده می‌شود، مثلِ فشارِ واقعیِ کلید.
  - **حذفِ سندِ باز، بوم را خالی نمی‌کند:** تا وقتی ترکش نکرده‌ای از بین نرفته؛ خالی کردنِ
    بوم یک نابودیِ دومِ نخواسته بود.
  - ⚠️ **در مرورگر دیده نشده:** modal، ردیف‌ها و حالتِ خالی را باید در روشن و تیره دید.

---

## 📄 فاز ۳ — مستندات از کد عقب افتاده‌اند

کارِ کم، ولی این‌ها چیزی‌اند که یک بازدیدکنندهٔ بیرونی **اول** می‌بیند.

- [x] **۳.۱ — [README.md](README.md)** ✅
  - «Toolbox: ۹ ابزار» → واقعاً **۱۲** (جدول، فیلدِ برچسب‌دار، فیلدِ صفحه اضافه شده‌اند).
  - «۵ قالبِ گالری» (invoice, sales report, letterhead, product label, certificate) →
    واقعاً **۲۲ قالب در ۶ دسته**.

- [x] **۳.۲ — [docs/designer-guide.md](docs/designer-guide.md)** ✅
  - §۳ «۹ ابزار» → ۱۲ · §۸ «۱۲ طرحِ آماده» → ۲۲.
  - §۹ بعد از فاز ۱.۵/۱.۶ باید حاشیه و واحد را هم توضیح دهد (و توصیهٔ «دستی ضرب در ۲.۸۳»
    حذف شود).

- [x] **۳.۳ — [TODO.md](TODO.md)** ✅
  - خطِ «جعبه‌ابزار ۹ الِمانی» → ۱۲.
  - آیتم‌های Phase 4 که این فایل پوشش می‌دهد به این‌جا ارجاع داده شوند (rulers، چندبندی،
    دستگیرهٔ چرخش، a11y — همه تکراری‌اند).

- [x] **۳.۴ — [ROADMAP.md](ROADMAP.md)** ✅
  - فاز ۵.۱ «Import معکوس PDF→قالب» ⬜ است ولی **کامل شده** (`cloneFromPdf`) و تا
    «عکس→قالب» هم رفته ([NEXT-TODO F4](NEXT-TODO.md)).
  - ROADMAP عملاً با NEXT-TODO جایگزین شده ولی **هیچ‌جا نوشته نشده** — از بیرون این‌طور
    به‌نظر می‌رسد که کلِ فاز ۵ دست‌نخورده است.
  - **کار:** یا ⬜ها را با واقعیت هم‌راست کن، یا یک یادداشتِ «superseded by NEXT-TODO.md»
    بالای فایل بگذار.

---

## 🏗️ فاز ۴ — بدهیِ ساختاری

- [x] **۴.۱ — تستِ واحد برای `designer.js`** ✅
  - **۴,۸۵۶ خط در یک IIFE با صفر تستِ واحد.** هر ۸۳ سوییتِ jest در `packages/` هستند؛
    دیزاینر فقط یک `smoke.js` دارد.
  - این **بزرگ‌ترین ریسکِ ساختاریِ فازِ بعدی** است: هر آیتمِ بالا در همین فایل زده می‌شود،
    بدونِ تورِ ایمنی.
  - **کار:** یک پروژهٔ jest سومِ `designer` با محیطِ jsdom؛ اول تستِ توابعِ خالص
    (`snapValue`, `nextSpot`, `dataPaths`, `labelFor`, `tplMatches`).
  - **پذیرش:** `npx jest` سه پروژه اجرا کند؛ گیتِ CI شامل آن شود.
  - **انجام‌شده:** پروژهٔ سومِ jest (`designer`) ثبت شد، پس `npx jest` حالا **۸۷ سوییت /
    ۷۵۰ تست** روی سه پروژه اجرا می‌کند و CI خودکار شاملش می‌شود (چون `npm test` همان
    `jest` است).
  - **مسئلهٔ اصلی و راهِ حل:** `designer.js` یک IIFE است، پس **هیچ‌چیزش از تست قابلِ
    دسترس نبود** — نه با require، نه با jsdom. به‌جای اینکه ۴.۲ را زودتر بکشم جلو،
    منطقِ **خالص** را به `designer-util.js` بردم: بدونِ DOM، بدونِ store، هر ورودی یک
    پارامتر. صفحه آن را قبل از `designer.js` بار می‌کند (`window.DesignerUtil`) و تست
    مستقیم require اش می‌کند. **یک پیاده‌سازی، دو مصرف‌کننده** — نه کپی.
  - **چه چیزی منتقل شد:** `snapValue` (+GRID/SNAP_EDGE) · تبدیلِ واحد (۱.۶) ·
    `resolveBandHeight`/`isPageWideBand` (۰.۱) · `RESIZE_EDGES` + **`resizeBounds`** ·
    `fitImageBox` (۱.۳) · `dataPaths`.
  - **بهترین بخش — `resizeBounds`:** ~۵۰ خطِ ظریف‌ترین هندسهٔ این نشست از داخلِ
    `mousemove` بیرون کشیده شد و **خالص** شد؛ snap به‌صورتِ callback تزریق می‌شود تا
    خطوطِ راهنما وارد هندسه نشوند. حالا هر شاخه‌اش تستِ خودش را دارد.
  - **۳۴ تست.** یکی‌شان چیزی را پیدا کرد که فکرش را نکرده بودم: رفت‌وبرگشتِ واحد **دقیق
    نیست** (۱۰۰pt = ۳٫۵۲۷۷cm → نمایشِ ۳٫۵۳ → ۱۰۰٫۰۶pt). ذاتیِ گرد کردن است، ولی تستِ
    دوم پین می‌کند که **انباشته نمی‌شود** — وگرنه هر بار باز کردنِ پنل چیدمان را کمی
    می‌لغزاند.
  - **mutation test:** برداشتنِ گاردِ وارونگی در `resizeBounds` → «never lets an edge
    cross its opposite» قرمز شد.
  - **گیت‌ها:** `npx jest` **۷۵۰ سبز** · designer:smoke (بدونِ تغییرِ رفتار: همان
    `svg bytes: 19167`) · eslint · typecheck · prettier — همه سبز.

- [ ] **۴.۲ — شکستنِ `designer.js` به ماژول** 🚧 _(شروع شد: منطقِ خالص + محتوا بیرون آمدند)_
  - بعد از ۴.۱ (تست اول، ریفکتور دوم). `build.mjs` از قبل esbuild دارد، پس باندل‌کردنِ
    چند فایل هزینه‌ای ندارد.
  - پیشنهادِ مرزها: `canvas/` · `inspector/` · `palette/` · `gallery/` · `copilot/` ·
    `persistence/`.
  - **🚧 دو برش بیرون آمد، الگو ثابت شد، بقیه مانده:**
    - `designer-util.js` (۴.۱) — منطقِ خالص.
    - **`designer-content.js`** — کلِ کپیِ کاربر (۱۰ بخشِ راهنما + ۸ قدمِ تور).
      دادهٔ خالص، صفر وابستگی؛ **۲۰۵ خط** از `designer.js` کم شد (۶٬۱۹۸ → ۵٬۹۹۳).
    - الگو: فایلِ مستقل با UMD wrapper → صفحه به‌صورت `<script>` بارش می‌کند، تست مستقیم
      `require` اش می‌کند. **یک پیاده‌سازی، دو مصرف‌کننده.**
  - **🚧 برشِ سوم — گالری (منطق، نه UI):** `tplNorm` · `tplTerms` · `tplMatches` ·
    `pageFormat` · `fitScale` · `faDigits` به `designer-util.js` رفتند، با **۲۴ تستِ واحد**.
    کپیِ فارسی در `designer.js` ماند (`pageFormat` تصمیم می‌گیرد _چه_ چیزی گفتنی است،
    نه اینکه _چطور_ گفته شود) — همان قاعدهٔ «کپی جای خودش».
  - **چرا UIِ گالری را جابه‌جا نکردم — و این تصمیم را با اندازه‌گیری گرفتم:** smoke فقط
    **۹ خط** دربارهٔ گالری داشت: باز شد، بسته شد، لود شد. جست‌وجو، فیلترِ دسته، پالت و
    کیبورد **هیچ تستی نداشتند**. جابه‌جا کردنِ ۳۶۰ خطِ تست‌نشده دقیقاً همان کاری است که
    ریفکتور را بی‌صدا خراب می‌کند — و خودِ ۴.۲ نوشته بود «تست اول، ریفکتور دوم».
    پس اول پوششش را ساختم: جست‌وجوی فارسی/عربی، فیلتر، حالتِ خالی، و پالت.
  - **🚧 برشِ چهارم — لینکِ اشتراک، و یک باگِ از‌دست‌رفتنِ داده که همین‌جا پیدا شد:**
    بخش را با **اندازه‌گیری** انتخاب کردم نه سلیقه — شمردم هر بخش چند بار در smoke
    ظاهر می‌شود: دادهٔ زنده ۱، اسنیپت ۲، **لینکِ اشتراک ۳**، بقیه ۶ تا ۱۳.
  - **باگ:** `hashchange` (یعنی paste کردنِ یک لینکِ اشتراک در دیزاینرِ باز) مستقیم
    `loadTemplate` می‌زد. قبل از ۲.۵ این فقط تاریخچهٔ undo را می‌سوزاند؛ **بعد از ۲.۵
    فاجعه شد**: سندِ باز حالا یک entry در کتابخانه دارد، پس autosayveِ بعدی قالبِ
    اشتراکی را **روی کارِ خودِ کاربر** می‌نوشت. حالا قالبِ اشتراکی — مثلِ قالبِ گالری —
    سندِ خودش می‌شود.
  - **کدکِ base64url به util رفت** با ۶ تستِ واحد. مهم‌ترینشان: خروجی باید **بایت‌به‌بایت**
    با پیاده‌سازیِ قبلی (`btoa(unescape(encodeURIComponent(s)))`) یکی باشد، وگرنه هر
    لینکی که تا امروز share شده می‌شکند. ضمناً chunk شد، چون
    `String.fromCharCode.apply` روی قالبِ ۱۰۰KB استک را سرریز می‌کند — دقیقاً روی
    بزرگ‌ترین سندها که بیشترین ارزشِ اشتراک را دارند.
  - **دو تستِ بی‌اثرِ خودم که mutation لو داد:** (۱) تستِ اول اصلاً به مسیرِ محافظت‌شده
    نمی‌رسید، چون تستِ حذفِ ۲.۵ قبلش `currentDocId` را null کرده بود — یعنی به دلیلِ
    غلط سبز بود. (۲) payload را خودم می‌ساختم، پس **خودِ دکمهٔ اشتراک هرگز تست نمی‌شد**؛
    mutation ای که کدک را با `identity` عوض کرد سبز ماند. حالا دکمه کلیک می‌شود و
    لینکِ واقعی‌اش decode می‌شود.
  - **🚧 برشِ پنجم — `designer-model.js`: خواندنِ مدل و نام‌گذاری‌اش.** این بار مرز
    «منطقِ خالص» نبود بلکه **تفسیر** بود: توابعی که جواب می‌دهند «این المان واقعاً چه
    می‌گوید» — `borderFacts` (یعنی `all` چه سمت‌هایی را روشن می‌کند)، `layerLabel`،
    `nextSpot` (اسکنِ فضای خالی)، `displayTemplate`، رنگ‌ها و آیکن‌ها. **۱۴۶ خط** از
    `designer.js` کم شد (۶٬۷۸۷ → ۶٬۶۴۱) با **۳۱ تستِ واحد**.
  - **دو تا `esc()` متفاوت پیدا شد** — یکی `& " <` را escape می‌کرد، دیگری `& < >`.
    هر کدام را برمی‌داشتی یک چیزی رد می‌شد، و اینکه کدام را می‌گرفتی به این بستگی
    داشت که کجای یک فایلِ ۶٬۰۰۰ خطی هستی. حالا یکی است و هر پنج کاراکتر را می‌گیرد.
  - **🚧 برشِ ششم — `designer-inspector.js`: پنل‌هایی که تابعِ مدل‌اند.**
    `renderInspector` یک‌جا جابه‌جا نمی‌شود (۵۰۰ خط که حین اجرا `store`/`selected`/DOM
    را می‌خواند)، ولی **دو پنلِ کاملِ داخلش خالص‌اند**: نوار و تنظیماتِ باند، و پنلِ جعبه.
    **۱۷۱ خط** بیرون رفت (۶٬۶۲۳ → ۶٬۴۵۱) با **۲۷ تستِ واحد**.
  - **چرا این دو تا، و چرا حالا — باز هم با اندازه‌گیری:** `model-coverage.spec.js` تضمین
    می‌کند هر پراپرتیِ استایل یک کنترل **دارد**؛ هیچ‌چیز تضمین نمی‌کرد **درست ساخته شده**.
    smoke فقط **۱۰ تا از ۴۴ کنترلِ** اینسپکتور را می‌زند و **هیچ‌کدام** از نُه کنترلِ پنلِ
    جعبه را. چک‌باکسی که `checked` ندارد، `<select>` ای که `selected` ندارد، و عددی که
    در سندِ میلی‌متری نقطه نشان می‌دهد — هر سه از grepِ پوشش رد می‌شوند و روی صفحه غلط‌اند.
  - **یک تله که همین‌جا بسته شد:** `model-coverage.spec.js` فقط `designer.js` را می‌خواند.
    با هر برشِ ۴.۲ بی‌صدا کور می‌شد — سبز می‌ماند چون دیگر نگاه نمی‌کرد. حالا فهرستِ
    فایل‌ها را می‌خواند و هر پنلِ جدید باید به آن اضافه شود.
  - ⚠️ **یک نکتهٔ ظریفِ jsdom:** `location.hash = …` خودش hashchange می‌زند، پس ست‌کردن
    **و** dispatch کردن یعنی دو بار اجرا و دو سند. با `replaceState` + یک dispatch
    دقیقاً همان چیزی می‌شود که paste کردنِ لینک تولید می‌کند.
  - **۶ mutation روی منطقِ گالری، هر ۶ قرمز** — از جمله «ی/ك عربی دیگر فولد نشود» و
    «بومِ خالی از فیلترِ دسته جان سالم به‌در نبرد».
  - **دو چیزِ کوچک که خودِ تست‌ها لو دادند:** یک assertionِ من روی `tplEmpty.hidden` بود
    در حالی که کد از کلاسِ `show` استفاده می‌کند — یعنی **تستِ اولم بی‌اثر بود** و
    assertionِ بعدی گرفتش. و انتخابگرِ پالتِ من `:not(.active)` بود در حالی که نشانه
    `aria-checked` است، پس داشت روی پالتِ _فعال_ کلیک می‌کرد.
  - **یک `var enteredGroup` تکراری** (دو بار پشتِ‌سرِ هم، با دو کامنتِ متفاوت) حذف شد.
  - ⚠️ **`esc` هم دوبار تعریف شده بود ولی باگ نیست:** دومی داخلِ یک تابع است، پس
    shadow می‌کند نه override. قبل از ادعا چک کردم.
  - **⚠️ صادقانه دربارهٔ باقی‌مانده:** فایل هنوز **~۶٬۲۰۰ خط** است و بقیهٔ بخش‌ها (بوم،
    اینسپکتور، گالری، کوپایلوت، persistence) بر خلافِ این دو، **حالتِ مشترک** دارند
    (`store`, `selected`, `zoom`, `activeBand`, `sampleData`). جدا کردنشان یعنی اول
    استخراجِ یک ماژولِ state.
  - **اندازه گرفتم که آن ماژولِ state چقدر بزرگ است:** `store` ۱۵۴ ارجاع، `selected` ۱۰۵،
    `zoom` ۷۲، `sampleData` ۲۷، `guides` ۲۱، `enteredGroup` ۱۶، `previewPage` ۱۵،
    `activeBand` ۱۳ — **~۴۳۰ نقطهٔ ارجاع**. یعنی بازنویسیِ تقریباً کلِ فایل، بدون هیچ
    سودِ کاربری، روی کدی که پوششِ تستش نقطه‌به‌نقطه یکسان نیست. **این را کورکورانه نمی‌زنم.**
    ترتیبِ درست: اول پوششِ هر بخش را بساز (مثلِ کاری که این‌جا برای گالری شد)، بعد
    ماژولِ state، بعد جابه‌جایی. هر بخش یک نشست.
  - **باگی که در همین مسیر پیدا شد:** راهنمای **داخلِ برنامه** هنوز «۹ ابزار» و
    «۱۲ طرح» می‌گفت — فاز ۳ فقط markdownها را دیده بود و این را نه، چون وسطِ ۶٬۰۰۰ خط
    دفن شده بود. هر سه ادعا اصلاح شد و **`content.spec.js`** حالا اعداد را با
    **خودِ منبع** می‌سنجد (شمارشِ `data-add` در HTML و `cat:` در `templates.js`).
    ⚠️ شمارشِ اولِ من هم غلط بود: الگوی `id:` تم‌های پالت و دسته‌ها را هم می‌گرفت و
    ۳۴ می‌داد؛ فقط یک قالبِ گالری `cat:` دارد.
    تست ضمناً می‌سنجد که هر selectorِ تور به المانی اشاره کند که **واقعاً در صفحه هست**
    (وگرنه تور بی‌صدا هیچ‌جا را هایلایت نمی‌کند) و اعداد به فارسی نوشته شده باشند.
    **mutation:** برگرداندنِ «۹ ابزار» → قرمز، با هر دو عدد در پیام.

- [x] **۴.۳ — قاعدهٔ «مدلِ جدید = کنترلِ جدید»** ✅
  - **شکافِ موتور↔UI سیستماتیک است، نه تصادفی.** موتور مدام رشد می‌کند (فاز ۰ راستگویی،
    نمودارهای RTL، بوردرِ خط‌چین…) و اینسپکتور همان ۴ کنترلِ ظاهری را دارد.
  - **کار:** یک تستِ نگهبان که فیلدهای `Typography`/`BoxStyle` را می‌خواند و assert می‌کند
    برای هرکدام یک `[data-prop]` در اینسپکتور وجود دارد (با allowlistِ صریح برای
    استثناها). تا این نباشد، فاصله فقط بازتر می‌شود.
  - **پذیرش:** افزودنِ یک property به مدل بدونِ کنترلِ متناظر، CI را قرمز کند.
  - **انجام‌شده:** `model-coverage.spec.js` نامِ فیلدهای `Typography` و `BoxStyle` را از
    **خودِ `style.ts`** می‌خواند (نه از یک کپیِ دستی که خودش کهنه می‌شود) و برای هرکدام
    یکی از این دو را می‌خواهد: یک کنترلِ نام‌برده در `designer.js`، یا یک **دلیلِ نوشته‌شده**
    که چرا نیست.
  - **جدولِ `EXPOSURE` نگاشتِ واقعی است، نه چک‌لیستِ اسمی:** یک property می‌تواند چند
    کنترل داشته باشد (`decoration` → `underline` + `strike`؛ `border` → شش فیلد، چون
    `BorderSet` یک شکلِ واحد است که با چند کنترل ویرایش می‌شود). تنها استثنا
    `fontFeatures` است، با دلیلی که **قابلِ اقدام** است: «pdf-lib راهی برای دادنِ
    featureها به فونتِ embedشده ندارد؛ رفعش fork کردنِ مسیرِ encode می‌خواهد» — دلیلی که
    نشود رویش کاری کرد صرفاً بهانه است، پس تست حداقلِ طول را هم چک می‌کند.
  - **در هر دو جهت گارد می‌دهد:** فیلدِ تازه در مدل بدونِ ورودی در جدول → قرمز؛ و ورودیِ
    جدول که کنترلش حذف/تغییرِ نام داده → قرمز.
  - **خودِ parser هم تست دارد** (۴ تست)، وگرنه اگر روزی `[]` برگرداند کلِ گارد **بی‌صدا
    خالی** می‌شد — همان الگوی «تستی که چیزی نمی‌سنجد».
  - **دو mutation test:** افزودنِ `textTransform` به مدل → «declares no field this test
    does not know about» با نامِ دقیقِ فیلد؛ تغییرِ نامِ `data-prop="letterSpacing"` →
    «letterSpacing has a control in the inspector».
  - **گیت‌ها:** `npx jest` **۷۷۳ سبز** (۸۸ سوییت) · designer:smoke · typecheck — سبز.

---

## 🏆 فاز ۵ — «چیا اضافه کنم بی‌رقیب بشه؟»

> این فاز **عمداً آخر است**: تا وقتی فاز ۰ و ۱ باز باشند، هر قابلیتِ درخشانِ جدید روی یک
> محیطی سوار می‌شود که کاربر نمی‌تواند درش لوگو بگذارد یا فونتش را عوض کند.
> **اول محیط را قابلِ استفاده کن، بعد خندق بکن.**

### 🥇 ۵.۱ — سامانهٔ مؤدیان (F3) — خندقِ واقعی

- [ ] **این تنها موردی است که محصول را از «ابزارِ خوب» می‌برد به «چیزی که کسب‌وکارِ ایرانی
      قانوناً لازم دارد».** هیچ کتابخانهٔ PDF ای در دنیا این را ندارد.
- **چرا ما:** رندرِ دترمینیستیک + هشِ کانونیک (F1) + مدلِ typed از قبل هست؛ امضا ادامهٔ
  طبیعیِ `verify/` است.
- زیرآیتم‌ها در [NEXT-TODO.md](NEXT-TODO.md) فهرست شده‌اند (۳.۱ تا ۳.۶) — این‌جا تکرار
  نمی‌شوند تا یک منبعِ حقیقت بماند.
- ⚠️ **مسدود بر سرِ یک تصمیمِ بیرونی، نه کار:** جزئیاتِ الگوها، کدِ یکتا و امضا
  **نباید از حافظه** پیاده شوند؛ مستندِ رسمیِ فعلیِ سامانه لازم است.
- **اگر فقط یک چیزِ بزرگ قرار است بزنی، این است.**

### 🥈 ۵.۲ — `<pdf-studio-designer>` به‌صورت Web Component

- [ ] تبدیل از «ابزار» به **«زیرساخت»**. هر SaaS ایرانی می‌تواند embed کند.
- باندلِ standalone (custom element) با API: property `template`، رویدادِ `change`،
  گزینهٔ white-label (لوگو/رنگ). همان دیزاینرِ فعلی.
- **پیش‌نیاز:** فاز ۴.۲ (ماژولار شدن) — وگرنه یک IIFE ۵ هزارخطی را باید در custom element
  بپیچی.
- **پذیرش:** دموی جاسازی در یک HTML خام + یک اپِ Angular؛ smoke.
- _(از [ROADMAP ۴.۱](ROADMAP.md))_

### 🥉 ۵.۳ — سرویسِ رندرِ Docker یک‌خطی

- [x] `docker run -p 3000 pdfstudio/render` → `POST /render {template, data} → PDF`. ✅
- Dockerfile روی dist موجود + `/healthz` + محدودیتِ اندازه/زمان.
- **پذیرش:** تستِ e2e با curl در CI (jobِ جدا).
- _(از [ROADMAP ۴.۲](ROADMAP.md))_
- **چرا این، نه ۵.۱ یا ۵.۲:** ۵.۱ (مؤدیان) **مسدود است بر سرِ مستندِ رسمی** — و خودِ آیتم
  نوشته «نباید از حافظه پیاده شود»، پس زدنش یعنی نقضِ شرطِ خودش. ۵.۲ (Web Component)
  **پیش‌نیازش ۴.۲ است** که هنوز کامل نیست (فایل ~۶٬۲۴۵ خط). ۵.۳ هیچ پیش‌نیازی ندارد.
- **انجام‌شده:** `apps/render-service/` — دو مسیر (`POST /render`، `GET|HEAD /healthz`)
  روی `node:http` **بدونِ هیچ فریم‌ورکی**؛ کلِ سطح دو route و یک محدودیتِ اندازه است،
  پس یک درختِ وابستگی بیشتر از چیزی که صرفه‌جویی می‌کند هزینه دارد.
- **مرزِ تست‌پذیری:** `service.js` نه `http` می‌شناسد نه موتور را — فقط **تصمیم** می‌گیرد
  (این بدنه چه شکلی دارد، این خطا چه statusای می‌خواهد). ۲۱ تستِ واحد رویش، بدونِ باز
  کردنِ پورت. `server.js` سوکت و رندر را دارد.
- **اعتبارسنجی با schemaِ خودِ موتور** (`importTemplate`)، نه یک schemaِ دومِ محلی که
  بعداً از آن جدا بیفتد. قالبِ نامعتبر → `422` **همراه با اینکه کجا غلط بود**.
- **محدودیتِ اندازه حینِ دریافت اعمال می‌شود، نه بعدش** — چک کردن بعد از buffer یعنی
  یک گیگابایت را از قبل به حافظه راه داده‌ای، که همان چیزی است که محدودیت برای جلوگیری
  از آن هست.
- **یک باگِ واقعیِ خودم که e2e گرفت:** نسخهٔ اولِ ۴۱۳ سوکت را **destroy** می‌کرد، پس
  کلاینت فقط یک قطعِ اتصال می‌دید و هیچ‌وقت نمی‌فهمید بدنه‌اش بزرگ بوده. حالا اول جواب
  می‌دهد، بعد قطع می‌کند.
- **۶ mutation، ۵ قرمز.** ششمی (`if (status === 413)`) **زنده ماند** — و درست بود که
  زنده ماند: مسیرِ عمومی هم همان ۴۱۳ را می‌فرستاد، یعنی آن شاخه فقط یک **پاسخِ تکراری**
  بود. ساده‌اش کردم به یک مسیرِ پاسخ، و صادقانه نوشتم که خودِ «قطعِ اتصال» تست نشده
  (کلاینتی می‌خواهد که پاسخ را نادیده بگیرد).
- **دو فرضِ غلطِ من که تست‌ها اصلاح کردند:** (۱) fixture ناقص بود (`page.unit`, `styles`,
  `datasets`, `parameters`, `resources.fonts/images`) — پیامِ خطای تست را بهتر کردم تا
  به‌جای «got 422» بگوید **چرا**. (۲) «فونت جاسازی شده» را با جست‌وجوی رشتهٔ `Vazirmatn`
  در بایت‌ها می‌سنجیدم؛ ولی pdf-lib از **object stream** استفاده می‌کند و نام فشرده
  می‌شود. حالا همان سند با فونتی که سرویس ندارد هم درخواست می‌شود و **اختلافِ دو پاسخ**
  دقیقاً همان جاسازی است.
- **دترمینیسمِ بایتی** به‌عنوان یک assertion: دو درخواستِ یکسان باید بایتِ یکسان بدهند.
- **ایمیج tarballِ بسته‌بندی‌شده را نصب می‌کند، نه workspace را** — پس چیزی که در کانتینر
  اجرا می‌شود همان artifactای است که مصرف‌کننده نصب می‌کند، و اشتباهِ بسته‌بندی همین‌جا
  می‌ترکد نه بعد از انتشار. e2e هم دقیقاً همین کار را می‌کند.
- ⚠️ **Docker build را نتوانستم اجرا کنم:** daemon روی این ماشین بالا نیست، پس
  **Dockerfile و مرحلهٔ Dockerِ CI اجرانشده‌اند**. منطقِ سرویس کاملاً تست شده (۲۱ واحد +
  ۱۹ e2e)، ولی خودِ ایمیج را CI اولین بار می‌سازد.

### ۵.۴ — همکاری زندهٔ چندنفره (Figma-for-PDFs)

- [ ] Yjs روی command streamِ موجود (`DocumentStore` آماده است) + سرورِ WebSocketِ کوچک +
      نشانگر/انتخابِ دیگران روی بوم.
- **صادقانه: کم‌بازده‌ترین موردِ این فهرست برای بازارِ هدف.** جذاب است، ولی هیچ‌کس به‌خاطرِ
  آن ابزارِ فاکتور عوض نمی‌کند. آخر از همه.
- _(از [ROADMAP ۵.۲](ROADMAP.md))_

---

## پیشرفت

| فاز                              | وضعیت     | یادداشت                                  |
| -------------------------------- | --------- | ---------------------------------------- |
| 🔴 ۰ — باگ‌های خاموش             | ۶/۶       | ✅ **فازِ ضروری کامل شد**                |
| 🟠 ۱ — موتور را قابلِ استفاده کن | ۱۳/۱۳     | ✅ **فاز کامل شد**                       |
| 🟡 ۲ — ارگونومی                  | ۵/۵       | ✅ **فاز کامل شد**                       |
| 📄 ۳ — مستندات                   | ۴/۴       | ✅ **فاز کامل شد**                       |
| 🏗️ ۴ — بدهیِ ساختاری             | ۲/۳       | ۴.۱ ۴.۳ ✅ — می‌مانَد ۴.۲ (ماژولار کردن) |
| 🏆 ۵ — بی‌رقیب شدن               | ۱/۴       | ۵.۳ ✅ — ۵.۱ مسدود، ۵.۲ منتظرِ ۴.۲       |
| **کل**                           | **۳۱/۳۵** | (۷ آیتم در جریانِ کار کشف و اضافه شدند)  |

### ترتیبِ پیشنهادیِ اجرا

1. ✅ **فاز ۰ بسته شد** (۰.۱ → ۰.۲ → ۰.۴ → ۰.۳ → ۰.۵ + ۰.۶ با هم).
   ⚠️ ۰.۴ قبل از ۰.۳ آمد چون زیرساختش (`laidBottom`) در ۰.۱ ساخته شد و کارِ کمی مانده.
   ۰.۵ و ۰.۶ هر دو **مالِ موتورند**، پس با هم و با `test:core` بهتر بسته می‌شوند.
2. **۱.۹** (۱۰ دقیقه‌ای) → **۱.۱ → ۱.۲** (تایپوگرافی و جعبه — بیشترین اثرِ دیداری)
3. **۱.۳ → ۱.۴** (لوگو و فونت — بیشترین درخواستِ واقعیِ کاربر)
4. **۱.۵ → ۱.۶ → ۱.۷** (صفحه: حاشیه، واحد، locale) → **۱.۸** (دستگیره‌ها)
5. **۳.۱ → ۳.۲ → ۳.۳ → ۳.۴** (مستندات — یک نشست)
6. **۴.۱** (تست) → **۴.۳** (نگهبان) → **۴.۲** (ماژولار)
7. ✅ **۲.۲ → ۲.۳ → ۲.۴** (پولیش) → **۲.۱** (بومِ کامل) → **۲.۵** (پروژه‌ها)
8. ✅ **۵.۳ Docker** (بدونِ پیش‌نیاز) — می‌مانَد **۵.۱ مؤدیان** (مسدود بر سرِ مستندِ رسمی)،
   **۵.۲ Web Component** (منتظرِ ۴.۲)، **۵.۴ همکاری**
