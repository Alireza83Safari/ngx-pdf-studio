# NEXT — TODO (فاز ۰ راستگویی · F3 مؤدیان · F4 خطِ لوله)

هر دو moonshotِ قبلی (F1 سندِ ضدِّجعل، F2 Format Cloner) کامل‌اند. این فایل ادامهٔ راه است.

> Legend: ⬜ شروع‌نشده · 🚧 در حال انجام · ✅ انجام‌شده
> ریتمِ کار: هر قدم → تست (`npm run test:core` + `npm run designer:smoke`) → lint/format → تیک.
> رندر: `createRenderContext → paginate → paint`. دو painter باید **هم‌خروجی** بمانند.

**ترتیبِ اجرا:** اول **فاز ۰** (باگ‌های تأییدشده — خروجیِ غلطِ بی‌سروصدا اعتماد را می‌کشد) → بعد **F3** (شرطِ بازار) → بعد **F4** (اتصالِ چیزهایی که از قبل داریم).

---

## 🧱 فاز ۰ — راستگوییِ رندر

**چرا اول:** هر موردِ زیر **از کد تأیید شده** است، نه حدس. مشترکشان یک الگوست: مدل چیزی را قبول می‌کند که painter پیاده نکرده، و به‌جای خطا، **بی‌صدا چیزِ دیگری می‌کشد**.

- [x] **۰.۱ — سه `chartKind` که دروغ می‌گویند** ✅
  - `combo`، `scatter`، `sparkline` در `ChartKind` و در `chartKindSchema` قبول می‌شوند، ولی
    [`chartOps`](packages/pdf-studio/core/src/paint/chart-ops.ts) هیچ شاخه‌ای برایشان ندارد →
    همه به ستونیِ گروهی می‌افتند. `ChartSeries.kind` هم **هیچ‌جا خوانده نمی‌شود**.
  - همین باگ در قالبِ «داشبورد مدیریتی» گرفته شد (سریِ «هدف» به‌جای خط، ستون کشیده می‌شد).
  - **تصمیم:** پیاده‌سازی، نه رد کردن — هر سه ارزانند و مدل از قبل شکلشان را دارد.
    - `combo`: به `series[i].kind` احترام بگذار (column/line/area روی یک محور).
    - `sparkline`: خطِ بدونِ محور/برچسب/راهنما، تمامِ کادر.
    - `scatter`: نقطه روی (اندیسِ دسته، مقدار).
  - **انجام‌شده:** `LaidChartSeries.kind` اضافه شد و `resolveChart` دیگر `s.kind` را دور
    نمی‌اندازد (قبلاً بی‌صدا حذف می‌شد). در `chart-ops.ts` رسم هر سری به چهار helperِ مشترک
    استخراج شد — `lineSeriesOps` / `areaSeriesOps` / `scatterSeriesOps` / `columnSeriesOps` —
    و شاخه‌های `line`/`area` و ستونیِ گروهی هم روی همان‌ها سوار شدند تا **یک** پیاده‌سازی
    بماند. `combo` هر سری را با شکلِ خودش روی **مقیاسِ مشترک** می‌کشد و فقط سری‌های ستونی برای
    اسلاتِ داخلِ دسته رقابت می‌کنند (پس خط/ناحیه رویشان می‌افتد، نه کنارشان). `sparkline` بدونِ
    محور/برچسب/راهنما تمامِ کادر را می‌گیرد. `scatter` مربعِ کوچک می‌گذارد (rect، تا هر دو
    painter یکسان بمانند). قالبِ داشبورد به `combo` واقعی برگشت. **۱۳ تستِ جدید**
    (`chart-kinds.spec.ts`: تفکیکِ شکل‌ها، اسلاتِ کامل برای تک‌سریِ ستونی، مقیاسِ مشترک،
    پیش‌فرضِ column، ناحیه در combo، سریِ خالی، عبور از هر دو painter). **۵۷۳ تستِ core سبز**
    (تنها شکستِ باقی‌مانده `fontkit-measurer` است که مسیرِ فونتِ لینوکسی می‌خواهد و از قبل
    روی ویندوز قرمز بود) + هر دو snapshot دست‌نخورده + typecheck/lint تمیز.

- [x] **۰.۲ — RTL بومی در موتور (نه در لایهٔ قالب)** ✅
  - `bounds.x` فیزیکی است، پس روی صفحهٔ RTL نویسندهٔ قالب باید دستی آینه کند. الان این کار در
    `baseTemplate`ِ `templates.js` انجام می‌شود — یعنی **هر کسی با API خودش قالب بسازد دوباره
    همان تله را می‌خورد** (سیزده قالبِ اولیه هم همین اشتباه را داشتند).
  - هدف: مختصاتِ **منطقی** در موتور (x=0 یعنی لبهٔ شروع)، یا دست‌کم یک helperِ صادرشده.
  - مهاجرت باید سازگارِ عقب باشد: قالب‌های موجود نباید جابه‌جا شوند (نسخهٔ schema یا فلگ).
  - **انجام‌شده:** فلگِ **اختیاریِ** `page.coordinates: 'physical' | 'logical'` (پیش‌فرض
    `physical` → هر قالبِ موجود بی‌حرکت می‌ماند، بدونِ نیاز به بالا بردنِ نسخهٔ schema).
    `layout/logical-bounds.ts` با `withLogicalBounds(template)` قبل از `paginate` در
    `layoutDocument` اجرا می‌شود — یعنی **یک** نقطه، پس هر دو painter و بومِ دیزاینر یکی
    می‌بینند. وقتی هیچ صفحه‌ای opt-in نکرده **همان reference برمی‌گردد** (مسیرِ رایج بدونِ
    allocation). فرزندانِ container و `itemTemplate`ِ list نسبت به جعبهٔ خودشان آینه می‌شوند و
    صفحهٔ چندستونی نسبت به **عرضِ ستون** (چون `paginate` باندها را در عرضِ ستون می‌چیند).
    صفحهٔ LTR دست‌نخورده می‌ماند، پس فلگ بی‌خطر است.
    **ترتیبِ مهم:** `withVerification` **قبل** از آینه اجرا می‌شود تا هشِ F1 روی قالبِ
    *نوشته‌شده* بماند و کدِ پنل = کدِ کاغذ = کدِ `verify.html` نشکند.
    `templates.js` دیگر mirrorِ خودش را ندارد؛ منطقی می‌نویسد و با `bakeLogical` همان
    helperِ موتور را یک‌بار صدا می‌زند و فلگ را برمی‌دارد (چون تابع **وارونِ خودش** است و
    وگرنه رندر دوباره آینه می‌کرد). دیزاینر عمداً فیزیکی می‌ماند چون overlayهایش را مستقیم از
    `bounds` می‌سازد. خروجی **بایت‌به‌بایت یکسان** با قبل (smoke: همان ۱۸۴۱۸ بایت).
    **۱۱ تستِ جدید** + بخشِ «Coordinates» در `docs/rtl-persian.md`. **۵۸۴ تستِ core سبز**.

- [x] **۰.۳ — نمودارهای RTL** ✅
  - دسته‌ها همیشه چپ→راست می‌چینند؛ روی سندِ فارسی باید راست→چپ باشد.
  - تخمینِ عرضِ راهنما `name.length * LABEL_SIZE * 0.62` لاتین‌محور است → برچسبِ فارسی می‌زند
    بیرون یا روی هم می‌افتد. باید از اندازه‌گیرِ واقعیِ متن (`layout/measure`) استفاده کند.
  - **انجام‌شده:** `LaidChart` سه فیلدِ **اختیاری** گرفت — `rtl`، `seriesNameWidths`،
    `categoryWidths` — و `resolveChart` جهتِ حل‌شدهٔ المان را می‌گیرد و عرضِ برچسب‌ها را با
    `deps.measurer` (همان فونتِ واقعیِ سند) اندازه می‌گیرد. `CHART_LABEL_SIZE` به لایهٔ layout
    منتقل شد چون حالا بخشی از قراردادِ **اندازه‌گیری** است (paint→layout مجاز است، برعکسش نه).
    در painter یک `slotOf(i, count, rtl)` معرفی شد و همهٔ helperهای سری یک `toSlot` می‌گیرند،
    پس ستون/خط/ناحیه/نقطه/انباشته **همگی** با دسته‌هایشان آینه می‌شوند — نه فقط برچسب‌ها.
    محورِ مقدار و برچسبِ بیشینه‌اش به لبهٔ شروع (راست) می‌روند، میله‌های افقی از راست رشد
    می‌کنند، راهنما از راست چیده می‌شود، و ستونِ راهنمای دایره‌ای به چپِ نمودار می‌رود.
    فیلدها اختیاری‌اند و `labelWidth` در نبودشان به همان تخمینِ قدیمی برمی‌گردد، پس
    `LaidChart`ِ دست‌ساز در تست‌ها هنوز کار می‌کند. **۸ تستِ جدید** (`chart-rtl.spec.ts`) +
    ۳۰ تستِ نمودارِ قبلی بدونِ تغییر سبز. **۵۹۲ تستِ core سبز.** چشمی هم روی داشبورد تأیید شد:
    فروردین→شهریور راست‌به‌چپ، خطِ قرمزِ هدف روی ستون‌ها، راهنمای دونات کاملاً خوانا.

- [ ] **۰.۴ — محورِ نمودار**
  - الان فقط **یک** برچسبِ max کشیده می‌شود (همان «۹۰۵»ِ سرگردون در داشبورد). نه tick، نه
    gridline، نه برچسبِ مقدار روی میله.
  - `showLabels` در مدل هست و خوانده نمی‌شود.

- [ ] **۰.۵ — بوردرِ تک‌ضلعی و خط‌چین در SVG**
  - `boxMarkup` می‌نویسد `border?.all ?? border?.top` و یک `<rect>` با یک stroke می‌کشد →
    بوردرِ فقط-راست/چپ/پایین در **پیش‌نمایش ناپدید** می‌شود، هرچند schema قبولش می‌کند.
  - `stroke.style` (`dashed`/`dotted`) در schema هست ولی SVG هیچ `stroke-dasharray` نمی‌دهد.
  - هر دو باید با painterِ PDF هم‌خروجی شوند (همان درسِ باگِ text-anchor).

**پذیرش فاز ۰:** هیچ مقداری که schema قبول می‌کند نباید بی‌صدا چیزِ دیگری بکشد — یا درست رندر
شود یا در validation رد شود. پیش‌نمایشِ SVG و PDF روی هر پنج مورد هم‌خروجی باشند.

---

## 🥇 F3 — سامانهٔ مؤدیان (سرتاسری)

**پیچ:** قالبِ `taxInvoice` امروز فقط ظاهر است — «کد یکتای مالیاتی» یک رشتهٔ ساختگی در دادهٔ
نمونه است. نسخهٔ واقعی: ساختِ payloadِ استاندارد از همان قالب، محاسبهٔ درستِ کد یکتا، امضای
دیجیتال، و ارسال به API.

**مو:** هیچ کتابخانهٔ PDF ای این را ندارد. این تنها موردِ فهرست است که محصول را از «ابزارِ خوب»
می‌برد به **«چیزی که کسب‌وکارِ ایرانی قانوناً لازم دارد»**.

**چرا ما:** رندرِ دترمینیستیک + هشِ کانونیک (F1) + مدلِ typed از قبل هست؛ امضا ادامهٔ طبیعیِ
`verify/` است.

- [ ] **۳.۱ — مدلِ صورتحساب** — تایپ + zod برای payloadِ استاندارد (الگوهای ۱..۷).
- [ ] **۳.۲ — نگاشتِ قالب → payload** — از dataset/بایندهای موجود، بدونِ دوباره‌نویسیِ داده.
- [ ] **۳.۳ — کد یکتای مالیاتی** — تولید و اعتبارسنجیِ واقعی (نه رشتهٔ نمونه).
- [ ] **۳.۴ — امضای دیجیتال** — PKCS#7؛ روی `verify/` سوار شود.
- [ ] **۳.۵ — کلاینتِ ارسال** — با حالتِ آفلاین/خشک برای تست.
- [ ] **۳.۶ — UX دیزاینر** — پنلِ «مؤدیان»: اعتبارسنجی قبل از ارسال، نمایشِ خطاهای سامانه.

> ⚠️ قبل از شروع باید مستندِ رسمیِ فعلیِ سامانه بررسی شود — جزئیاتِ الگوها و امضا تغییر می‌کنند
> و **نباید از حافظه پیاده شوند**.

---

## 🥈 F4 — خطِ لوله: عکس → سندِ امضاشده

**پیچ:** سه قطعه از قبل ساخته شده و کسی به هم وصلشان نکرده:
`Format Cloner` (F2 ✅) ساختار را از PDF درمی‌آورد · `Copilot` ✅ می‌تواند بایند کند ·
`Verify` (F1 ✅) مهرِ ضدِّجعل می‌زند.

**مو:** «۱۰ ثانیه‌ای هر سندی را بدزد» که در MOONSHOT نوشته شده الان **نصفه** است — نصفهٔ دوم
(استقرارِ خودکار + امضا) نسبت به اثرش کارِ کمی است.

- [ ] **۴.۱ — عکس → قالب** (Stretchِ F2.6): OCR/ویژن → همان pipelineِ `cloneFormat`.
- [ ] **۴.۲ — یک دکمه:** انداختنِ فایل → قالب + دادهٔ نمونه + مهرِ تأیید، در یک حرکت.
- [ ] **۴.۳ — تشخیصِ لوگو → المانِ تصویر** (از Stretchِ F2).

---

## پیشرفت

- فاز ۰ راستگویی: ۳/۵ 🚧
- F3 مؤدیان: ۰/۶
- F4 خطِ لوله: ۰/۳
- **کل: ۳/۱۴**

> بعدی: **۰.۴** — محورِ نمودار (tick/gridline/`showLabels`).
