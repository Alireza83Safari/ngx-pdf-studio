# Design Review — TODO دیزاینر (PDF Studio)

کارهای برآمده از **بازبینی طراحی UI/UX** دیزاینرِ درون‌مرورگری.
هدف: رساندن رابط از **۱۴/۲۰** به سطحِ یک SaaS جهانی.
دامنه: تقریباً همه‌چیز در `apps/playground/designer/designer.html`
(به‌جز موارد نشان‌دار که به `designer.js` مربوط‌اند).

> گزارش کامل: نقدِ ۱۲بخشیِ منتشرشده (کارت امتیاز، ایرادها، مقایسه‌ها).
> امتیازها: UI 15 · UX 16 · Visual 14 · **A11y 9** · Consistency 13 · Pro 16.

**Legend:** ⬜ شروع‌نشده · 🚧 در حال انجام · ✅ انجام‌شده
**رفرنس خط‌ها** نسبت به نسخهٔ فعلیِ `designer.html` است؛ بعد از هر ویرایش ممکن است جابه‌جا شوند.

**ریتمِ کار (هر آیتم):** ویرایش → `npm run designer:build` → باز کردن `designer.html` و چکِ چشمی → تیکِ باکس.

---

## 🔴 دستهٔ ۱ — ضروری (Accessibility · بلاکرِ انتشار)

این چهار مورد چیزی‌اند که «رد» را از «قبول» جدا می‌کنند. اول این‌ها.

- [x] **۱.۱ — کنتراستِ متنِ کم‌رنگ (`--faint`)** ✅
  - `--faint: #98a1b2` روی سفید = **۲.۶:۱** (حداقل AA = ۴.۵:۱)، و برای عنوانِ بخش‌ها با فونتِ ۱۰.۵px استفاده شده.
  - کجا: توکن لایت خط ~۱۶، دارک خط ~۴۴؛ مصرف در `.sec-title`, `.hint`, `.tinyhint`, `.mast .meta`…
  - کار: `--faint` لایت → حدود `#6b7280` (≈۴.۶:۱)؛ دارک را هم بازبررسی کن. کمینهٔ فونتِ متنِ خوانا را از ۱۰.۵px به **۱۲px** ببر.
  - پذیرش: هیچ متنی روی surface زیر ۴.۵:۱ نباشد (متنِ بزرگ ≥۳:۱).
  - **انجام‌شده:** لایت `#98a1b2 → #6b7280` (**۴.۸:۱**)، دارک `#5b6473 → #8a93a3` (**۵.۲:۱**). ⚠️ بمپِ فونت به ۱۲px به آیتم **۳.۱** (مقیاسِ تایپ) موکول شد تا چیدمان یکجا و امن اصلاح شود.

- [x] **۱.۲ — کنتراستِ دکمهٔ primary (به‌ویژه دارک)** ✅
  - `.btn.primary { color:#fff }` (خط ~۲۶۷) توکنِ `--accent-ink` را نادیده می‌گیرد. دارک: سفید روی `#5b94f6` = **۳.۰:۱** (رد). لایت: روی `#3b76f6` = **۴.۱:۱** (مرزی).
  - کار: `color:#fff` → `color: var(--accent-ink)`. (`--accent-ink` دارک = `#0d1117` → ≈۶:۱.)
  - پذیرش: متنِ CTA در هر دو تم ≥۴.۵:۱.
  - **انجام‌شده:** `.btn.primary` + `#tourCard button.primary` + `.help-head .hh-ico` همه → `var(--accent-ink)`. ضمناً accentِ لایت `#3b76f6 → #2f6bf0` تا سفید روی آن ≥۴.۵:۱ شود (لایت **۴.۷:۱**، دارک **۶:۱**). ⚠️ تغییرِ جزئیِ رنگِ برند — اگر نپسندیدی برمی‌گردانیم.

- [x] **۱.۳ — بوم و لایه‌ها با کیبورد قابل‌انتخاب شوند** ✅
  - `.el` (خط ~۴۴۲) و `.layer` (خط ~۹۵۵) هر دو `div` بدون `tabindex`/`role`.
  - کار: به ردیف‌های لایه `role="button" tabindex="0"` + هندلرِ `Enter`/`Space` برای انتخاب بده؛ روی بوم `Tab`/`Shift+Tab` بین المان‌ها بچرخد و انتخاب کند. (nudge با فلش‌ها از قبل هست.)
  - پذیرش: بدونِ ماوس بشود یک المان را انتخاب، جابه‌جا و حذف کرد.
  - **انجام‌شده:** هم `.el` و هم `.layer` حالا `role="button"` + `tabindex="0"` + `aria-pressed` دارند. تابعِ مشترکِ `selectById` + هندلرهای Enter/Space (با refocus بعد از رندر). چون فلش/Delete/Ctrl+D در سطحِ document روی `selected` کار می‌کنند، انتخاب که شد، جابه‌جایی و حذف هم بدونِ ماوس کاملند. ضمناً `selectById` حالا `renderStatus()` را هم صدا می‌زند تا نوارِ وضعیت (کلیک + کیبورد) درست شود. **۱۰/۱۰ چکِ jsdom با اجرای واقعیِ فلو سبز.**

- [x] **۱.۴ — معناشناسی و Focus-Trap مُدال‌ها** ✅
  - مُدال‌ها: `#gallery`, `#help`, `#copilot`, `#history`, `#palette` (خط‌های ~۲۲۲۸–۲۳۵۴). هیچ‌کدام `role="dialog"`/`aria-modal` ندارند و فوکوس trap نمی‌شود.
  - کار: به هر مُدال `role="dialog" aria-modal="true" aria-labelledby=…`؛ حلقهٔ focus-trap (Tab دورِ عناصرِ داخل)؛ بعد از بستن، فوکوس به دکمهٔ بازکننده برگردد. (Escape و کلیکِ backdrop از قبل کار می‌کنند.)
  - پذیرش: با Tab نمی‌شود به پشتِ مُدال رفت؛ بستن فوکوس را بازمی‌گرداند.
  - **انجام‌شده:** هر ۵ مُدال `role="dialog" aria-modal="true"` + `aria-labelledby`/`aria-label` گرفتند. تابعِ `installModalA11y()` در `designer.js` با یک MutationObserver روی کلاسِ هر مُدال: هنگامِ باز شدن Tab را trap و فوکوس را داخل می‌برد، هنگامِ بستن به بازکننده برمی‌گرداند — بدونِ دست‌زدن به نقاطِ پراکندهٔ `.show`. (`node --check` + smoke سبز.)

- [x] **۱.۵ — احترام به `prefers-reduced-motion`** ✅
  - هیچ بلوکِ reduced-motion وجود ندارد؛ ترنزیشن‌ها و انیمیشنِ تور/نورافکن بی‌قید اجرا می‌شوند.
  - کار: `@media (prefers-reduced-motion: reduce) { * { transition-duration: .01ms !important; animation: none !important; } }` + کوتاه‌کردنِ transitionهای `#tourRing`.
  - پذیرش: با روشن‌بودنِ کاهشِ حرکتِ سیستم، حرکتِ محسوسی نباشد.
  - **انجام‌شده:** بلاکِ سراسریِ reduced-motion قبل از `</style>` اضافه شد (transition/animation صفر + `#tourRing` بدونِ transition).

---

## 🟡 دستهٔ ۲ — مهم (Responsive · Consistency · Feedback)

- [ ] **۲.۱ — Responsive / چیدمانِ سازگار** ⬜
  - صفر `@media` در فایل. عرض‌های ثابت: rail 56 / aside 300 / preview 400. زیر ~۱۰۲۴px بوم خفه می‌شود؛ موبایل/تبلت می‌شکند.
  - کار: زیرِ یک breakpoint، پنل‌ها (aside/preview) به overlayِ جمع‌شونده بروند؛ حداقل یک حالتِ tablet سالم.
  - پذیرش: در عرضِ ۷۶۸px و ۳۷۵px بدونِ اسکرولِ افقیِ بدنه و بدونِ قطع‌شدنِ کنترل‌ها.

- [x] **۲.۲ — یکدست‌سازیِ آیکون‌ها (حذفِ اموجی)** ✅
  - اموجی قاطیِ SVGهای تمیز: `🔢 مقادیر` (~۱۷۴۲)، `✨ کوپایلوت` (~۱۷۵۶)، `⌘K` (~۱۷۴۹)، `🌙` تم (~۱۸۰۹)، `؟` راهنما (~۱۸۱۱). همچنین اموجیِ داخلِ مُدال‌ها/تیترها.
  - کار: همه به یک ست SVGِ stroke-based هم‌سبکِ آیکون‌های موجود (۱۶–۱۸px، `currentColor`).
  - پذیرش: هیچ اموجی‌ای به‌عنوانِ آیکونِ کنترل نماند.
  - **انجام‌شده:** کلِ کنترل‌های topbar به SVG رفتند — مقادیر (`#`/tag)، فرمان‌ها (کنسول، جایگزینِ `⌘K` که cross-platform هم شد)، کوپایلوت (sparkle)، تم (ماه/خورشیدِ SVG، swap در JS)، راهنما (help-circle). تیترِ مُدالِ کوپایلوت و دکمهٔ «بساز» هم پاک/SVG شدند. توابعِ `applyTheme`/`updateValuesBtn` حالا `innerHTML` با SVG می‌گذارند. اموجیِ **نثری/دکوراتیو** (flourishِ empty-state «بوم خالیه ✨»، calloutهای راهنما 👋💡🎨، hintهای palette، فلش‌های ⬆⬇ کوییک‌بار، ✕ بستن، ✓ ذخیره) عمداً کنار گذاشته شد — آیکونِ کنترل نیستند.

- [x] **۲.۳ — ارقامِ یکدست (فارسی/لاتین)** ✅
  - `zoomLabel` با `Math.round(zoom*100)+'%'` لاتین است (js خط ~۱۱۳۵)، اما دکمهٔ «۱:۱» فارسی و «A4» لاتین → قاطی در یک نوار.
  - کار: یک تابعِ واحدِ `toLocaleDigits` و اعمال روی همهٔ اعدادِ نمایشی؛ یا تصمیمِ آگاهانه «همه‌جا لاتین».
  - پذیرش: سبکِ ارقام در کلِ نوارِ وضعیت یکسان.
  - **انجام‌شده (تصمیم: chrome/readout = لاتین):** بررسی نشان داد همهٔ readoutهای زندهٔ نوار (zoom٪، ابعادِ pt، شمارندهٔ انتخاب، A4) از قبل لاتین‌اند؛ تنها لیبلِ ثابتِ «۱:۱» فارسی بود. آن را در دکمهٔ نوار + فرمانِ palette + ارجاعِ راهنما به «1:1» بردم. ارقامِ فارسیِ داخلِ متنِ راهنما/تولتیپ‌ها نثر است و عمداً فارسی می‌ماند.

- [x] **۲.۴ — حالت‌های Loading (spinner + aria-busy + skeleton)** ✅
  - تنها بازخورد، متنِ status/toast است. دکمهٔ «بساز ✨» فقط با `cpBusy` قفل می‌شود (js خط ~۳۵۰۴) ولی ظاهرش قابل‌کلیک می‌ماند. گالری/پیش‌نمایش/دریافتِ داده skeleton ندارند.
  - کار: کلاسِ استانداردِ `.is-loading` (spinner + `disabled` بصری + `aria-busy`) برای اکشن‌های async؛ skeletonِ shimmer برای گالری و پیش‌نمایش.
  - پذیرش: هر اکشنِ طولانی حالتِ درحال‌انجامِ بصریِ روشن دارد.
  - **انجام‌شده:** CSS اسپینر (`.btn.is-loading::after`) + helperِ `setLoading(btn,on)` (spinner + `aria-busy` + قفلِ re-entry) به هر سه اکشنِ async وصل شد: **دانلود PDF**، **کوپایلوت (بساز)**، **دریافتِ دادهٔ زنده**. **۱۰/۱۰ چکِ jsdom با اجرای واقعیِ فلو (fetchِ stub‌شده) سبز.** skeletonِ گالری/پیش‌نمایش لازم نشد چون هر دو از دادهٔ محلی و **همگام** رندر می‌شوند (loading واقعی ندارند).

- [ ] **۲.۵ — صفحهٔ «پروژه‌ها / خانه»** ⬜ (بزرگ‌تر — می‌تواند جدا فاز شود)
  - الان کاربر مستقیم واردِ ویرایشگر می‌شود؛ مدیریتِ چند سند نیست.
  - کار: یک صفحهٔ خانه: اسنادِ اخیر، جست‌وجو، «سند نو»، ورود از گالری. (روی همان localStorage/history موجود سوار شود.)
  - پذیرش: بشود بینِ چند سند جابه‌جا شد بدونِ از دست رفتنِ کار.

---

## 🟢 دستهٔ ۳ — اختیاری (Design-System · Polish)

- [ ] **۳.۱ — توکنیزه‌کردنِ مقیاسِ تایپوگرافی** ⬜
  - ~۱۴ سایز و پُر از نیم‌پیکسل (10.5/11.5/12.5…). بدونِ مقیاسِ مدولار.
  - کار: مقیاسِ ۶پله (مثلاً 11/12/13/15/18/22) به‌صورتِ توکن و نگاشتِ همهٔ font-sizeها.

- [ ] **۳.۲ — توکنیزه‌کردنِ فاصله و شعاع** ⬜
  - پدینگ/گپ خارج از گریدِ ۴/۸ (۳،۵،۷،۹،۱۱،۱۳)؛ شعاع ۱۲+ مقدارِ متفاوت (1…18).
  - کار: مقیاسِ فاصلهٔ ۴/۸پایه (`--sp-1..`) و شعاعِ ۴مقداری (`--r-sm/md/lg/xl`).

- [ ] **۳.۳ — استانداردسازیِ ارتفاعِ کنترل‌ها** ⬜
  - هفت ارتفاعِ دکمه (24/26/28/30/32/34/36).
  - کار: سه سایزِ استاندارد (مثلاً 28/34/40) و نگاشتِ همه.

- [ ] **۳.۴ — کنترل‌های سفارشی (color / select)** ⬜
  - `input[type=color]` و `select`های بومی با زبانِ طراحی جور نیستند.
  - کار: color-picker و select سفارشیِ هم‌سبک (یا حداقل `appearance`-based یکدست).

- [ ] **۳.۵ — Toast بهتر** ⬜ · _`designer.js`_
  - toastِ واحد، بدونِ آیکون/واریانتِ موفق، بدونِ اکشنِ درون‌خطی.
  - کار: واریانت‌های success/error/info با آیکون + دکمهٔ «واگرد» درونِ toast (به‌جای فقط ذکرِ Ctrl+Z). js خط ~۱۳۶.

- [ ] **۳.۶ — Elevation و Motion توکنیزه** ⬜
  - سایه‌های دستیِ موازی؛ مدت/easingِ پراکنده.
  - کار: نردبانِ سایهٔ ۳پله + `--ease`/`--dur-*` و مصرفِ یکدست.

- [x] **۳.۷ — حلقهٔ فوکوسِ هم‌شعاع** ✅
  - `:focus-visible` همه‌جا `border-radius:8px` (خط ~۱۰۲–۱۰۶)؛ با ابزارِ شعاع‌۱۱ ناهم‌خوان.
  - کار: `border-radius: inherit` یا outline بدونِ شعاعِ ثابت.
  - **انجام‌شده:** `border-radius: 8px`ِ اجباری حذف شد تا outline از شعاعِ خودِ هر المان پیروی کند.

- [x] **۳.۸ — Scrollbar فایرفاکس** ✅
  - فقط `::-webkit-scrollbar` (خط ~۸۲–۹۴).
  - کار: `scrollbar-width: thin; scrollbar-color: var(--border-strong) transparent;`.
  - **انجام‌شده:** روی `html` گذاشته شد (خصیصه‌های `scrollbar-*` ارث‌بری می‌شوند → همهٔ scroll containerها).

- [x] **۳.۹ — یک مکانیزم Tooltip** ✅
  - هم `[data-tip]` و هم `title=` بومی (chipهای فیلد) → احتمالِ نمایشِ هم‌زمان.
  - کار: `title`ها را حذف یا به `data-tip` منتقل کن.
  - **انجام‌شده:** `upgradeTooltips()` از قبل `title`→`data-tip` را همه‌جا اعمال می‌کند جز `renderCanvas`. تنها گپ، `title`ی بومیِ `.el` بود که حذف شد (نوعِ المان از طریقِ `aria-label` + placeholder می‌آید). حالا تک‌مکانیزم.

- [ ] **۳.۱۰ — Empty stateِ اکشن‌دارِ Inspector** ⬜
  - Emptyِ Inspector فقط متن است (خط ~۱۷۴۴ در js).
  - کار: یک دکمهٔ واقعی («یک المان اضافه کن» / «از قالب شروع کن») به‌جای فقط متن.

---

## پیشنهادِ ترتیبِ اجرا

1. **۱.۲** (تک‌خطی، اثرِ زیاد) → **۱.۱** → **۱.۵** (سریع، CSS)
2. **۱.۴** (ARIA + focus-trap مُدال‌ها) → **۱.۳** (کیبوردِ بوم/لایه)
3. **۲.۲** (آیکون) → **۲.۳** (ارقام) → **۳.۷/۳.۸/۳.۹** (پولیش‌های سریعِ CSS)
4. **۳.۱ → ۳.۲ → ۳.۳ → ۳.۶** (توکنیزه‌کردنِ سیستم — با هم منطقی‌اند)
5. **۲.۴** (Loading) → **۳.۵** (Toast) → **۳.۴** (کنترل‌های سفارشی) → **۳.۱۰**
6. **۲.۱** (Responsive — بزرگ) → **۲.۵** (صفحهٔ پروژه‌ها — بزرگ، فازِ جدا)

پس از اتمامِ هر دسته: یک بار `npm run designer:smoke` و بازبینیِ چشمی در لایت/دارک.

---

## پیشرفت

- 🔴 ضروری: ۵/۵ ✅ **دستهٔ ضروری کامل شد**
- 🟡 مهم: ۳/۵ (۲.۲، ۲.۳، ۲.۴ ✅ — می‌مانَد ۲.۱ Responsive، ۲.۵ صفحهٔ پروژه‌ها)
- 🟢 اختیاری: ۳/۱۰ (۳.۷، ۳.۸، ۳.۹ ✅)
- **کل: ۱۱/۲۰**
