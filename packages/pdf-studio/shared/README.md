# shared (internal workspace package)

Design tokens, model re-exports, and shared utilities consumed by both `core`
and the Angular library. **Not published standalone** — its surface is
re-exported through `@ngx-pdf-studio/core` / `@ngx-pdf-studio/angular`
(ADR-0005).

**Status:** scaffolded. Populated as the designer's token system lands in
**Phase 4A** (§8A design-token system).
