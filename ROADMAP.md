# 🚀 نقشهٔ راه «بی‌رقیب شدن» — ngx-pdf-studio

هدف: PDF-سازی که به بقیه نگاه هم نکنن. به‌ترتیب از بالا می‌زنیم؛ هر آیتم باید
**کامل + تست‌شده + کامیت‌شده** تحویل بشه بعد بریم سراغ بعدی.

وضعیت‌ها: ⬜ نزده · 🚧 در حال انجام · ✅ تمام

---

## فاز ۱ — پکِ ایران 🇮🇷 (مالکیت کامل بازار فارسی)

> چرا اول؟ وابستگی خارجی صفر، کاملاً در کنترل خودمان، و هیچ رقیب جهانی ندارد.

### ۱.۱ ✅ تابع «عدد به حروف» فارسی — `toWords()`

- **چیست:** `toWords(4850000)` → «چهار میلیون و هشتصد و پنجاه هزار»؛
  `toWords(x, {currency: 'rial'})` → «… ریال». هر فاکتور رسمی این را می‌خواهد.
- **کجا:** تابع whitelisted جدید در `expression/functions.ts` + پشتیبانی اعشار (ممیز)،
  عدد منفی، و مقیاس تا هزار میلیارد.
- **پذیرش:** تست واحد کامل (صفر، مرزها، اعشار، منفی، ارز) + مستند در
  `docs/expression-language.md` + استفاده در قالب فاکتور.

### ۱.۲ ✅ قالب «فاکتور رسمی مالیاتی» + QR سامانه مودیان

- **چیست:** قالب منطبق با فرم صورتحساب فروش کالا/خدمات (سربرگ فروشنده/خریدار با
  شناسهٔ ملی/اقتصادی/کدپستی، جدول ۱۴ستونه‌ٔ استاندارد، ردیف مالیات بر ارزش افزوده،
  جمع به حروف با `toWords`) + جای QR کد مالیاتی (مقدار از دیتا: `taxid`).
- **کجا:** `templates.js` (گالری) + دادهٔ نمونهٔ واقعی.
- **پذیرش:** صفر issue/diagnostic؛ خروجی PDF چاپی تمیز A4؛ smoke گالری آپدیت.

### ۱.۳ ⬜ فرمت‌های مالی فارسی داخلی

- **چیست:** `format: { kind: 'money' }` → سه‌رقم جداکن + «ریال/تومان» +
  گزینهٔ پرانتز برای منفی (حسابداری). میان‌بر روی `formatNumber` موجود.
- **پذیرش:** تست فرمت + گزینه در اینسپکتور دیزاینر (سلکت «قالب نمایش» برای فیلد).

### ۱.۴ ⬜ Justify با کشیدهٔ فارسی (تطویل) — دموی افسانه‌ای

- **چیست:** تراز دوطرفهٔ متن فارسی با درج «ـ» (U+0640) در محل‌های مجاز اتصال،
  به‌جای فقط فاصله. در richText/staticText با `align: 'justify'`.
- **کجا:** `layout/`측 خط‌شکن + هر دو painter؛ قواعد اتصال از fontkit
  (حروف قابل‌اتصال) — fallback به فاصله وقتی خط کشیده‌پذیر نیست.
- **پذیرش:** تست golden با استخراج pdfjs؛ مقایسهٔ بصری در showcase؛ سوییچ خاموش/روشن.
- **ریسک:** متوسط — قواعد کشیده ظرافت دارد؛ نسخهٔ v1 = درج در آخرین اتصال هر کلمه.

---

## فاز ۲ — بردهای سریع دیزاینر ⚡ (هر کدام ۱–۲ جلسه)

### ۲.۱ ⬜ اشتراک قالب با لینک

- قالب فشرده (deflate+base64url) در `#hash` صفحهٔ دیزاینر؛ دکمهٔ «اشتراک لینک» →
  کپی در کلیپ‌بورد؛ در لود، hash → import + validate.
- **پذیرش:** لینک یک قالب کامل گالری < ۸KB؛ smoke رفت‌وبرگشت.

### ۲.۲ ⬜ پیش‌نمایش با دیتای زنده (URL)

- در تب داده: فیلد «آدرس API» → fetch + جایگزینی sampleData (با دکمهٔ refresh و
  مدیریت خطا/CORS). حالت آفلاین دست‌نخورده بماند.
- **پذیرش:** smoke با mock fetch؛ خطای شبکه → toast + دیتای قبلی حفظ.

### ۲.۳ ⬜ UI شرط‌ها (موتورش هست!)

- در اینسپکتور: بخش «نمایش شرطی» (`visibleWhen`) + «استایل شرطی» ساده
  (یک قانون: شرط + رنگ/ضخیم). موتور از قبل کامل پشتیبانی می‌کند.
- **پذیرش:** روی بوم زنده اثر کند؛ smoke: شرط false → المان از SVG حذف.

### ۲.۴ ⬜ تاریخچهٔ نسخه‌ها

- Snapshot خودکار هر N تغییر در localStorage (حداکثر ۲۰) + پنل «نسخه‌ها» با
  بندانگشتی زنده و «بازگردانی».
- **پذیرش:** بازگردانی undoable باشد؛ smoke.

---

## فاز ۳ — کوپایلوت هوش مصنوعی 🤖 (هدلاین محصول)

### ۳.۱ ⬜ معماری + provider

- ماژول `copilot/` جدا: interface `generate(prompt, currentTemplate?) → templateJson`؛
  providerهای قابل‌تعویض (Claude API پیش‌فرض، قابل‌تنظیم با کلید کاربر در UI).
  حلقهٔ **validate→repair**: خروجی LLM → `importTemplate` → اگر issue داشت،
  issueها را برگردان به مدل (حداکثر ۲ تلاش).
- **پذیرش:** تست با mock provider؛ هیچ خروجی invalid ای به بوم نرسد.

### ۳.۲ ⬜ UI چت در دیزاینر

- پنل «✨ کوپایلوت»: پرامپت + حالت‌ها («بساز از صفر» / «قالب فعلی را تغییر بده»)،
  پیش‌نمایش زندهٔ نتیجه قبل از اعمال، اعمال = یک command واحد (undoable).
- **پذیرش:** smoke با mock: پرامپت → قالب روی بوم؛ undo برگرداند.

### ۳.۳ ⬜ پیشنهاد بایند هوشمند

- «دیتا را بخوان و فیلدهای قالب را خودت وصل کن» — تطبیق نام/نوع + گزارش تغییرات.

---

## فاز ۴ — تبدیل به زیرساخت 📦

### ۴.۱ ⬜ `<pdf-studio-designer>` به‌صورت Web Component

- باندل standalone (custom element) با API: `template` property،رویداد `change`،
  گزینهٔ white-label (لوگو/رنگ). همان دیزاینر فعلی، قابل‌جاسازی در هر محصولی.
- **پذیرش:** دموی جاسازی در یک HTML خام + Angular؛ smoke.

### ۴.۲ ⬜ سرویس رندر Docker یک‌خطی

- `docker run -p 3000 pdfstudio/render` → `POST /render {template, data} → PDF`؛
  Dockerfile روی dist موجود + `/healthz` + محدودیت اندازه/زمان.
- **پذیرش:** تست e2e با curl در CI (job جدا).

---

## فاز ۵ — جادوها 🪄

### ۵.۱ ⬜ Import معکوس: PDF → قالب قابل‌ویرایش

- استخراج متن+مختصات با pdfjs → الِمان‌های staticText با موقعیت دقیق؛
  تشخیص خطوط/جدول‌های ساده v2. دکمهٔ «باز کردن PDF» در منوی فایل.
- **پذیرش:** فاکتور خروجی خودمان را import کنیم → قالب نزدیک به اصل.

### ۵.۲ ⬜ همکاری زندهٔ چندنفره (Figma-for-PDFs)

- Yjs روی command stream موجود (DocumentStore آماده است)؛ سرور WebSocket کوچک؛
  نشانگر/انتخاب دیگران روی بوم.
- **پذیرش:** دو تب مرورگر همزمان؛ تست همگرایی commandها.

---

## یادداشت اجرا

- بعد از هر آیتم: تست/smoke + prettier + کامیت انگلیسی + به‌روزرسانی همین فایل (⬜→✅).
- گیت‌های همیشگی: `npx jest`، `node apps/playground/designer/smoke.js`، lint/format.
- سنگین‌های مستقل (PDF/A، PAdES، HarfBuzz…) در `TODO.md` می‌مانند؛ این فایل فقط مسیر «تک شدن» است.
